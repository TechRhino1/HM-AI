# 2026-09-17

## Session 19 — "use mt5 real data": the platform was reading synthetic bars

User answered the P0-1 gate question with **"use mt5 real data"** and the backlog question with
**"all, one by one"**. This session took the first item: make the platform actually read the live
terminal.

### The misdiagnosis I nearly shipped

`/api/telemetry_state` reported `services.MT5 = DISCONNECTED` and the dashboard said **"broker
offline"**. MT5 was in fact connected (package 5.0.6180, terminal build 6182, account 101059540,
equity 728.36). I started reading the data path looking for a product defect.

The process was started with
`start_server(host='127.0.0.1', port=8501, mt5_client=None, orchestrator=None)` — **my own
CSS-verification harness**, which has no broker client at all, so `DISCONNECTED` was *correct*. Found
it by reading the running pid's cmdline (`psutil.Process(7728).cmdline()`), not by reading code.
**Check how the process was started before diagnosing the data path.**

### The three real defects (commit `9c99483`)

1. **Execution mode gated market data.** `MT5Client.init_connection()` early-returns for
   `paper`/`backtest`/`offline`, and *nothing else* called `mt5.initialize()`. So in the process where
   the feed runs the terminal was never initialized → `resolve_broker_symbol()` failed every probe →
   the canonical `XAUUSD` (not the broker's `GOLD.i#`) reached `copy_rates_from_pos` → 0 rows →
   **every frame `SYNTHETIC_FALLBACK`**. Confirmed by logging the orchestrator's own data path:
   `MT5 returned 0 rates for XAUUSD (D1/H4/H1/M15). Falling back to synthetic rates.` and
   `mt5.terminal_info(): False`, while a bare `mt5.initialize()` in a fresh process made
   `resolve_broker_symbol('XAUUSD')` return `GOLD.i#` and a fetch return 5 rows in 0.00s.
   Fix: `broker_symbols.ensure_mt5_terminal()` (cached, retried at most every 30s), called by
   `data_feed._fetch`. **Measured: paper mode went from 0/20 live frames to 20/20.**
2. **A failed probe was cached forever.** `_FAILED[sym] = True` is indistinguishable from "the broker
   has no such symbol", so one call before the terminal was ready pinned every symbol to `None` for
   the process lifetime. No longer cached when the terminal is down.
3. **The health flags were inferred, not measured.** `DATA_FEED` was keyed off the *execution*
   account login, so paper mode said `OFFLINE` forever while real bars streamed — and `dashboard.js`
   read `broker online/offline` off the same flag. Now `DataFeedEngine.data_health()` measures it
   (`STREAMING|STALE|CLOSED|SYNTHETIC|OFFLINE`) and the chip keys off real bars being present.

### Three more found while verifying

* **Fabricated bars were being traded on.** `WTI` is in `Orchestrator.symbols` and absent at XM, so
  the feed synthesised a WTI series and the platform analysed it as real. A live session now refuses
  a `SYNTHETIC_FALLBACK` frame — **scoped to `terminal_ready()`**, so it fires only when we *have* a
  working broker link that still answered nothing. With the terminal down the synthetic frame is kept
  for the UI and execution is already blocked (`get_account_snapshot()` → `login 0`).
  This is the case the 2026-09-16 note deliberately left ungated; the scope guard is what makes it
  safe to gate now.
* **Cold history reads as a stalled feed.** MT5 pulls history on demand, **asynchronously**. Measured
  on 6 untouched symbols: **5 read ~19.6h stale, unchanged across 7 back-to-back calls (30-47ms)**,
  then `FRESH` a few hundred ms later. So the freshness gate would refuse a good symbol on the first
  cycle after every restart — intermittently, and only right after a restart. Bounded warm-up added
  (1.2s budget, 0.3s poll, paid once per symbol).
* **`COPPER` resolved to an EQUITY.** The fuzzy scan was `if sym in n.upper()`, so `COPPER` matched
  `SouthernCopper` (Southern Copper Corp) and was returned as the copper commodity — stamped
  `LIVE_MT5` and certified `FRESH`, so nothing downstream could tell. Now prefix-only + logged at
  WARNING.

Also: the 0-rates and refusal warnings repeated on every scan (**21+ lines/cycle** for one
unavailable symbol across 3 styles); both are now once-per-key, as `_stale_warned` already was.

### Verified

`tests/test_market_data_independence.py` — **14 tests** (paper mode must still ask for the terminal;
an uninitialised terminal must not poison the cache; the warm-up both ways — a cold symbol recovers
*and* a genuinely stale feed still reports STALE; `COPPER` must not resolve to an equity).
pytest **830 passed / 20 deselected** (+14, no regressions); ui_live 44/44; render 88/88; nav 31/31;
layout 238/238.

Restarted the real platform (`HM_start.py paper`, `JARVIS_ENABLE_TUNNEL=0`) and confirmed live:
`MT5 terminal initialized for market data.`; `services.MT5=CONNECTED`, `services.DATA_FEED=STREAMING`;
real XM account 101059540 equity 728.36; 12 symbols with decisions and **WTI correctly absent**.

Pushed `9c99483`, remote verified via `git ls-remote` (matches local HEAD).

### MEMORY.md consolidation

MEMORY.md was 9.2 KB and truncating on injection again. Split by topic rather than compressed again:
the displaced project conventions and the "rules that keep biting" moved into **`TRAPS.md`** (now
21 KB, read on demand), and MEMORY.md is now a **3.8 KB index** (58% smaller) — pointer table first,
then non-negotiables, running instructions, baselines. Reordered so the most critical content
survives if the tail is cut. Every pointer target verified to exist.

### Still open

The four backlog items, one by one (user: "all, one by one"): **P0-1** re-measure the gate on this
now-real data; **P0-3** derive stop/target/time-stop from ATR percentiles + measured MFE;
**P1-3** re-scan M15+M5 over the full window; **P2-1** Deflated Sharpe / sample-uniqueness. Plus the
read-side dedup guard. Also `BOOTSTRAP.md` still needs the user's **name** and **city**.

## Session 20 — P0-1: measuring the gate on real MT5 data

User answered the P0-1 gate question with **"use mt5 real data"** and the backlog question with
**"all, one by one"**.

### Data basis verified first

All 20 `data/market/real/<SYM>/<SYM>_H1_365d.parquet` manifests: `provenance: MT5_TERMINAL_REAL`,
`synthetic: false`, `full_window: true`, `quality.ok: true`, **0 issues**, 5,723–8,760 rows. So the
measurement basis is genuinely real terminal data, not the stored-synthetic path.

### Two new tools

* **`tools/p0_1_gate_measurement.py`** — replaces the arbitrary `PF >= 1.3` bar with the question
  that actually decides the gate: *is mean R per trade distinguishable from zero after costs?*
  Three traps handled: (1) trades within a symbol are **not independent**, so the aggregate treats
  each **symbol as a cluster** (n=20) rather than pooling ~95k trades; (2) 20 symbols is 20
  hypotheses, so the bar for a "best symbol" is the **distribution of max|t|** across 20 null draws,
  not 1.96; (3) a symbol can look good from after-the-fact selection. Includes a self-contained
  Student-t p-value (verified against 8 known values) and seeded bootstraps.
* **`tools/p0_1_direction_audit.py`** — separates **direction skill from beta**: same entries, same
  risk distance, same target multiple, LONG only. If the gate's mean R is no better than always-long,
  the P&L came from the instrument's drift.

### The decisive finding: the winners are beta

At 365d, the three symbols that cleared the multiple-testing bar were **all beaten by simply being
long**: XAUUSD gate +0.0889R vs always-long +0.1128R; XAGUSD +0.0402 vs +0.0744; WTI +0.0229 vs
+0.0871. So the "POSSIBLE SIGNAL" verdict from stage 1 is an artefact of gold/silver/oil trending.

Stability check killed the rest: every "direction adds value" verdict **flips with the window**
(XAUUSD 183d adds value → 365d beta; ETHUSD 183d loses → 365d adds value; BTCUSD, NAS100, USDCAD all
flip). Only "gate loses" is stable. The one symbol profitable *and* beating always-long at both
windows is GER40, at +0.044R/+0.017R — too small and too inconsistent to act on.

Aggregate: cluster-robust **t = −1.78, p = 0.091, 95% CI [−0.0555, +0.0008] contains 0**, point
estimate **negative**. Of the 13 symbols whose CI excludes zero, **10 are significantly LOSING** and
only 3 winning. Note the pooled per-trade t = −6.27 is *inflated* and would have been the wrong
number to quote.

### But the measurement was contaminated — found mid-flight

`signal_scan.py` added the spread **for BUY only**:

```python
entry_price = float(nxt["open"])
if bias == "BUY":
    entry_price += spread_pips * spec.pip_size      # SELL: nothing
```

`trade_simulator` mentions spread only in its docstring (no exit-side spread term), so **every SELL
candidate in every cached table entered at the mid and carried no spread at all**. Verified on the
stored tables: `(fill − next_open)/pip` ÷ row spread = **+1.000 BUY, 0.000 SELL** on every symbol
checked. `engine.py` already had the correct form *with the comment explaining it* — the scanner was
missed. Classic "two entry points, one fixed".

It also explains something that looked like a finding: SELL mean R exceeded BUY mean R on 13/20
symbols, which is exactly what a free short leg produces.

**Fix (`fdf8e58`)**: `entry_fill()` in the new `jarvis/backtesting/fills.py` is the single
implementation; `engine.py` and `signal_scan.py` both call it. `tests/test_entry_fill_convention.py`
(11 tests) includes `test_no_module_reimplements_the_fill`, which greps the tree for the arithmetic
outside `fills.py`. Five tests fail on the pre-fix code, all SELL-side. pytest **841 passed** (+11).

**Blast radius: EVERY cached candidate table is affected** — `H1_365d`, `H1_183d`, `H1` 95d
(`candidates.parquet`, the calibration input), and `M15_183d` + `M5_183d` (which feed
`fit_score_calibration`, regime optimisation, and are P1-3's target). So the fitted calibration and
all SCALP/DAY_TRADING numbers are contaminated too, not just P0-1's.

### Trap worth remembering

The re-scan writes **one file per symbol as it finishes**, so a partially-complete re-scan silently
produces a **mixed** dataset — and a measurement run against it looks plausible and moves only
slightly, which is exactly how you talk yourself into believing a null result. I compared mtimes with
`--time-style=+%H:%M:%S`, which **drops the date**, and read 15 stale files as fresh. Always print the
full date, and check `ls -l` counts before measuring on a regenerated cache.

## Session 21 — P0-3 stage A: the stop's noise band, measured

P0-1 is blocked behind the re-scan, so I started **P0-3** (derive stop/target/time-stop from
volatility instead of constants). Wrote `tools/p0_3_exit_geometry_measurement.py` with four
independently runnable parts. **Part A needs no candidate data** — pure OHLC — so it ran against the
live cache while the re-scan was still going.

**Measured, `noise = |close−open| / ATR` (causal ATR: bar *i* uses ATR as of *i−1*), 20 symbols:**
the ratio is **symbol-invariant and timeframe-invariant**. Body median 0.322–0.373 across FX, JPY
crosses, gold, silver, oil, three indices and three cryptos — a ±0.025 spread over four asset
classes. Adverse q90: **1.431 (H1) / 1.428 (M15) / 1.406 (M5)**.

**The decisive table — false-stop rate = P(bar's own adverse range > k×ATR):**

| k | H1 | M15 | M5 |
|---|---|---|---|
| 0.85 | 16.9% | 16.9% | 17.2% |
| 1.00 | 12.2% | 12.1% | 12.2% |
| 1.11 | 10.0% | 10.0% | 10.0% |
| 1.25 | 7.3% | 7.1% | 7.0% |

So **one universal `k ≈ 1.11` buys a 10% mechanical false-stop rate on every timeframe and every
symbol.** The style caps below it are the defect:

* **SCALP `min(0.65×ATR, …)` → 28.3%** — the only materially broken one. `dynamic_levels.py:174,303`.
* DAY_TRADING index `min(0.90×ATR, …)` → 15.3%; forex `1.05` → 11.1%; other `1.30` → 6.6%.
* SWING structural fallback `0.85–1.00×ATR + 0.35 buffer` = 1.20–1.35 → **8.3% / 6.1%** — fine.

**The spec's "scale by ATR percentile" clause is measurable but negligible.** corr(rolling k, ATR
percentile) is positive in **20/20** symbols (median +0.234, so the effect is real, not noise) but the
magnitude is +5.2%: rolling q90 k goes 1.033 → 1.087 from the bottom ATR quintile to the top. That
moves the false-stop rate ~1pp. Adding a scaling term would be complexity for nothing. **Do not
implement it** — report why.

**Self-correction worth keeping:** my first measure took `max(up, down)` — the worst case for *any*
stop. But a BUY stop sits below and a SELL above, so for a balanced book the honest figure is the
*mean* of the two one-sided rates. That inflated SCALP from **28.3% → 53.7%**, a 1.9× overstatement.
Both are now reported; only the directional one is quoted. An overstated defect number is as damaging
as an understated one.

**Next for P0-3:** parts B (tp_r sweep vs vol bucket), C (max_bars edge decay), D (cost gate) all need
the clean cache — the SELL-spread fix moved `fill`, and `risk_dist = |entry_fill − sl|`, so `risk_dist`
moved too. Do not run them on the mixed cache. Then the *acceptance test* for changing the SCALP cap
is whether the measured payoff actually improves with a wider stop — not the false-stop rate alone.

## Session 22 — P0-1 COMPLETE (both windows, clean cache) and P0-3 stages B/C

The re-scan finished: **365d 20/20 and 183d 20/20, all files dated 2026-09-17, 46,939 candidates.**
Only then did I re-measure. Both caches verified by date count before running anything.

### P0-1 — the corrected numbers, and the verdict

| Statistic | free SELL leg (invalid) | both legs pay |
|---|---|---|
| mean R per trade | −0.0243 | **−0.0509** |
| pooled t (inflated — do not quote) | −6.13 | −12.92 |
| **cluster-robust t** (symbols as units) | −1.77 | **−3.04** |
| p | 0.094 | **0.0067** |
| 95% CI on the mean | [−0.0546, +0.0011] | **[−0.0835, −0.0201]** |
| PF ≥ 1.0 / ≥ 1.3 | 7/20 / 0/20 | 6/20 / 0/20 |

**The verdict flips.** With the free short leg the CI contained zero; with both legs paying it
excludes zero. The loss grew 0.0266R, implying a one-spread cost of 2 × 0.0266 ≈ 0.053R — matching a
~1.5-pip spread against a ~24-pip stop. Independent confirmation: the side asymmetry **inverted**,
from SELL-better on 13/20 to SELL-worse on 12/20, which is exactly what removing a one-sided subsidy
must do.

**Stage 2, both windows — this is the decisive result.** (Figures below are *after* correcting the
control; see the bias note at the end of this section.)

| symbol | 365d edge vs always-long | 183d edge | stable? |
|---|---|---|---|
| BTCUSD | +0.0560 | −0.0833 | FLIPS |
| ETHUSD | +0.1097 | −0.0523 | FLIPS |
| EURUSD | +0.0895 | −0.0250 | FLIPS |
| GBPUSD | +0.0610 | −0.0874 | FLIPS |
| SOLUSD | +0.0773 | −0.0508 | FLIPS |
| USDCAD | +0.0337 | −0.0273 | FLIPS |
| XAGUSD | −0.0324 | +0.0661 | FLIPS |
| XAUUSD | −0.0222 | +0.1304 | FLIPS |
| **GER40** | **+0.0183** | **+0.0473** | **STABLE +** |
| **NAS100** | **+0.0057** | **+0.0273** | **STABLE +** |
| **NZDUSD** | **+0.1661** | **+0.0589** | **STABLE +** |
| 9 others | negative | negative | stable (loses) |

**8/20 verdicts flip when the window is halved.** The two statistics that matter:

* **Mean edge over always-long: −0.0068R (365d, cluster t=−0.36) and −0.0501R (183d, t=−2.35).**
  On average the gate's directional call adds nothing, and probably subtracts.
* **3/20 symbols beat always-long in both windows.** Under a no-edge null with independent windows
  you would expect 20 × 0.25 = **5**; because 183d is *inside* 365d the true null expectation is
  higher still. **Observing 3 is not above chance.**

**The gate has no reliable directional edge.** This is not a tuning problem; it is the entry.

### Bias found and fixed in my own control (worth remembering)

`forced_long_benchmark` entered the control long at the candidate's stored `fill`. For a BUY row
that is the ask (correct), but **for a SELL row `fill` is the bid** — so always-long was getting a
free half-spread, biasing the comparison *against* the gate. Conservative, but still wrong, and on a
2.3-pip FX spread it is worth ~0.05R/trade — the same order as the effects being measured.

Fixed to `fill + 2 × spread` for SELL rows. Validation: NZDUSD moved **+0.0988R (365d)** and
**+0.1129R (183d)**, against an expected half-spread of ~0.09R for a ~0.19R FX round turn. The
correction moved the *verdict*: survivors went from 1/20 (GER40 only) to 3/20, and flips from 7 to 8.
**A biased control can hide a real effect as easily as invent it** — fix it, don't footnote it.


### P0-3 stage B/C — the target and the clock are NOT the constraint

Added an **always-long control to every sweep point**, because without it the tp sweep measures
drift, not the target. XAUUSD, uncontrolled, said "best tp=6, E=+0.33R" — a wider target with a
fixed stop and a 200-bar clock just holds longer on a rising instrument. With the control:

* **tp sweep, 1.0R → 6.0R: the gate beats always-long at 0 of 8 levels.** The gap *widens* as the
  target widens (−0.019R at tp1 → −0.166R at tp6). Widening the target helps beta more than it
  helps the gate.
* **max_bars sweep, 12 → 1000: edge negative at every clock**, best at mb=12 (−0.0135R).
* The audit's specific claim *is* confirmed: `avg_win/tp` rises 0.928 (mb200) → 0.986 (mb1000), so
  the clock does truncate winners. But it is ~7% of the target and **removing the clock helps beta
  more than the gate** (mb1000: gate +0.2688 vs always-long +0.3470).

So P0-3's "target from measured MFE" will not rescue the signal. Only the **stop** finding is a
real defect (SCALP's 0.65×ATR cap → 28.3% false-stop).

**Method trap, again, and it is the same shape as the last one:** a sweep without a benchmark
produced a plausible, monotone, entirely spurious "best parameter". The fix is not a better sweep —
it is a control. Ask of every measurement: *what else would produce this number?*

**Launched:** `scan_signals.py` for H1 95d (the calibration input), then M15 183d, then M5 183d —
background task `S3Me9H`. M15 has ~5.7× the bars of H1 and M5 ~17×, so this is a long job.

## Session 23 — P2-1 built, and a real broker-offset defect found via 4 red tests

### P2-1: what already existed, and what did not

`PurgedKFold` (embargo) already exists in `jarvis/learning/walk_forward.py`, and
`jarvis/learning/sample_weights.py` claimed to implement López de Prado's sample-uniqueness
weighting. **The Deflated Sharpe Ratio was genuinely missing.** But the existing uniqueness code had
two defects that made it unusable, both now fixed:

1. **Wrong quantity.** It returned `1 / (concurrency at the label's start)`, never looking past the
   first bar of the span. A 1-bar label and a 10-bar label sharing a start must score 0.50 and 0.95 —
   the long label is alone for nine of its ten bars. The old code returned **0.5 for both**.
2. **O(n²) with a Python double loop.** 4,000 labels took 3.5s, so the audit's 94,937 trades would
   have taken **~33 minutes**. It was never run at scale, which is why defect 1 survived.

Now vectorised — difference array for concurrency, prefix sum for the span means, O(n + T). 20,000
labels take **0.010s**. `entry_edge_diagnostic._uniqueness_weighted` gets the fix for free.

Also dropped `|pnl|` rescaling from the default path: that is return-magnitude weighting, not
uniqueness weighting, and folding it in biased expectancy upward by over-weighting big winners. Still
available as `magnitude_weighting=True`.

New `jarvis/learning/deflated_sharpe.py` (PSR, DSR, expected-max Sharpe, min track-record length,
`effective_trials`). 33 new tests in `tests/test_sample_uniqueness.py` + `tests/test_deflated_sharpe.py`.
**Six of my first-draft test expectations were wrong and the implementation was right in all six** —
they forced me to derive the closed forms properly. The best of them: I asserted the PSR reduces to a
bare z-test for normal returns, but the denominator is `sqrt(1 + SR²/2)` (Lo 2002), not 1. Had I
"fixed" the code to match my wrong test, the DSR would have been silently wrong.

### A real defect, found because 4 tests went red

`pytest` went from 841 → **874 passed, 0 failed**. But first, 4 tests failed, and chasing them found
a genuine bug rather than a test artefact.

**`broker_utc_offset` returned 0 for every caller**, for two independent reasons:

1. It called `symbol_info_tick` **without initialising the terminal in this process**. The execution
   client only initialises for modes that place orders, so any read-only process got `None` for every
   symbol — the same class of defect as the market-data path.
2. It was handed **canonical** symbols. MT5 knows gold as `GOLD.i#`; `symbol_info_tick("XAUUSD")` is
   `None`. (The data path passes the broker symbol, but `broker_utc_offset` is called from elsewhere
   with canonical names.)
3. And a third, found while fixing: the real caller passes **one** symbol, and a single slow symbol
   breaks the derivation. Gold's tick ran ~17 min behind EURUSD's, landing 799s from the nearest
   30-minute boundary against a 300s tolerance → rejected as stale. The offset is a property of the
   **server**, so any fresh tick answers the same question; it now retries across liquid majors.

After the fix, from a cold process: `XAUUSD → 10800`, `NAS100 → 10800`, `EURUSD → 10800`, `WTI → 10800`
(WTI has no broker symbol at all). Before: all `0`.

**Why this mattered more than the holding-duration bug it was written for:** `classify_bar_freshness`
computes `age = (now + offset) − bar_epoch`, and bars are stamped on the broker clock. With
`offset = 0`, `age` came out **negative** — the bars looked like they were in the future — which the
classifier maps to `UNKNOWN`. And **`first_stale_frame` only blocks on `STALE`, never `UNKNOWN`**, by
design, so synthetic frames can still be reasoned on. Net effect: a zero offset **silently disabled
the stale-feed gate**. The four tests were passing *because of the bug*; fixing the offset made the
ages truthful and gold's genuinely-stale M5/M15 frames were then correctly refused.

**The trap:** two independent safety mechanisms shared a single failure mode. A check that fails
*open* is worse than no check, because it reports success. `offset=0 → age<0 → UNKNOWN → gate
tolerates` is exactly that shape, and it hid a stale feed for as long as it existed.

The 4 tests were then fixed properly — they mock the decision/execution engines and assert radar and
learning plumbing, but still fetched live bars first, so their outcome depended on the wall clock and
the broker connection. Each now neutralises **only** `first_stale_frame`, with a comment saying why.

### Still open

* P0-3 parts B/C/D at 19/20 (task `CExQt6`); M15 rescan (task `S3Me9H`) still running.
* `UNKNOWN` on a `LIVE_MT5` frame is currently tolerated, and that is what allowed the above. Worth
  deciding deliberately: "I have real bars but cannot verify their age" is not the same statement as
  "these bars are synthetic", yet the gate treats them alike.


## Session 24 — P0-3 B/C/D, P2-1 DSR, P1-3 refit (2026-09-17, 03:00)

### Backlog closed out

**P0-3 B/C/D measured** (H1 365d, 20/20, `reports/p0_3_exit_geometry.json`). Full tables in
`AUDIT-2026-09.md`. Headlines: the target and the clock are **not** the constraint; the cost gate
rules out 5/20 symbols outright (SOLUSD 90.1%, NZDUSD 81.8%, USDCAD 61.5%, AUDUSD 51.5%, USDCHF
50.6% of a 1R target). The audit's `avg_win/tp` claim reproduces exactly — 0.9285 → 0.9855 on XAUUSD
at mb 200 → 1000 — but relaxing the clock moves the edge over always-long **not at all**
(−0.0389 → −0.0388R), because it helps always-long just as much as the gate. The argmax tp lands on
tp1 for 8 symbols and tp6 for 4: no clustering, i.e. fitted noise.

**Two errors of mine in that measurement, both of which changed the answer:**

1. The always-long control was **biased** — it entered at the candidate's stored `fill`, which is the
   ask for a BUY row but the **bid** for a SELL row, handing the control a free half-spread. Fixed to
   `fill + 2 × spread` for SELL rows. Verified the 2× two ways: against `fills.entry_fill` (a SELL is
   charged `bid − spread` to recover the missing exit spread, so the ask really is `fill + 2×spread`)
   and empirically against the tables. Survivors **1/20 → 3/20**.
2. Part D overstated cost by **exactly 10×** — MT5's `spread` is in *points*. AUDUSD read 1.91R per
   trade instead of 0.19R. Fixed to consume the candidates' own `spread_pips`.

**P2-1 DONE — and it fails everything.** `tools/deflated_sharpe_report.py` →
`reports/deflated_sharpe_{365d,183d}.json`. `n_trials` is the grid the search walked and
`var(trial SR)` is measured, so the deflation is computed, not assumed. 5/20 pass at 365d on the raw
row count; **0/20 on effective n**; 0/20 at the production tp. The portfolio's 94,937 rows are worth
**327 independent bets (0.3%)** — quoting the row count overstated the evidence **290×**.

*The search was never the main problem.* Deflating for the tp grid alone still leaves XAUUSD 1.000,
XAGUSD 1.000, ETHUSD 0.996, BTCUSD 0.992, USDCAD 0.982. What removes them is the overlap: a
candidate is emitted on nearly every bar and `max_bars=200` keeps hundreds of positions open at once.
On effective n the same five fall to 0.844 / 0.754 / 0.664 / 0.675 / 0.621. Best case WTI needs
**4,985 trades** to be distinguishable from the best of this search; it has 4,133.

**P1-3 refit does not move the verdict.** Every table regenerated after `fdf8e58`: H1 365d/183d/95d +
M15 183d = 94,937 / 46,939 / 18,998 / 182,508 candidates (M15 took 1,708s; M5 running). Calibration
refit → **0/20 skillful in both windows**, and the extremes shrink as n doubles (max |AUC − 0.5|
0.087 → 0.055, against 0.062 expected from pure noise). There is no signal for a larger sample to
converge to — which is the honest reading of the earlier 0/20, not a shortage of data.

### Verified, not assumed

* 95d covers **16** symbols because EURJPY, GBPJPY, US500 and WTI have **no `H1_95d` bar cache** —
  checked the filesystem rather than guessing a reason.
* The `forced_long_benchmark` 2× correction checked against `fills.entry_fill` *and* empirically
  (`(fill − open)/pip` ≈ ±spread for FX, and the sign flips with side).
* pytest **874 passed / 20 deselected**, unchanged.

### Still open

* M5 183d rescan running (started 03:02).
* P0-3: the SCALP 0.65×ATR cap — must clear the payoff test, not just the false-stop rate.
* `UNKNOWN` on a `LIVE_MT5` frame still tolerated (session 23).
* Read-side dedup guard.
* `BOOTSTRAP.md` still needs the user's name and city.

## Git: committed + pushed the P0 audit work

All 30 files were already staged (no untracked residue). Commit `48af885` — "audit: P0
signal-quality measurements (deflated Sharpe, direction audit, exit geometry)": 23,861 insertions
across `jarvis/learning/deflated_sharpe.py`, `tools/p0_1_*`, `tools/p0_3_*`, sample-uniqueness
weighting, and the broker-time / data-feed / server / orchestrator fixes carried out of the audit.
Pushed to `origin/main` (`9146e91..48af885`); `git ls-remote` confirms remote `main` == `48af885`.

Push timing was **3m09s**, not the ~40s previously recorded — corrected in `MEMORY.md`. The push does
eventually succeed, so impatience (or a retry) is the only real failure mode here.

## M5 183d rescan: healthy, ~half done — and it is sitting on a live integrity trap

`tools/scan_signals.py --days 183 --timeframe M5 --workers 4` (pid 16704) started 03:02. As of
03:45 it had written 10 of 20 candidate tables (last `NZDUSD` 03:42:55) with all four workers at
93–94% of a core. Not stalled — my first CPU read said 0% and was wrong (`cpu_percent(None)`; see
TRAPS.md). It should finish on its own; leave it alone.

**Do not run an M5 backtest until it completes.** `data/signals/scan_manifest_M5.json` is still the
2026-09-13 single-symbol WTI manifest: `since='2026-07-06'`, 1 symbol. The current run passes no
`--since`, so its candidate tables are indexed against the *untrimmed* 183d frame. Until the manifest
is rewritten (it is written last, after all symbols), `optimizer.manifest_since()` replays that
`since` as a trim, and because the trim is alignment-critical every `bar_idx` lands on the wrong bar
across all 20 symbols — not just a shortened window. Written up under Data integrity in `TRAPS.md`.

**Measured, not inferred:** full M5 frame 37,440 bars (2026-03-15 → 2026-09-11); the stale
`since=2026-07-06` cut it to 14,400; fresh EURUSD candidates ran to `bar_idx` 37,438. The optimizer's
own guard refuses `bar_idx >= len(frame)` and logs "trim replay is wrong", so gross breakage is
caught — but a shift small enough to stay in range is still silent.

**Fixed at source** (`ee88571`): `tools/scan_signals.py` now publishes the manifest *before* the first
candidate table lands, with the correct `since` plus `complete: false`, then rewrites it with the
symbol summaries and `complete: true` at the end. `manifest_path()` keeps the existing naming so
`optimizer.manifest_since()` reads the same file. The optimizer additionally warns when it sees
`complete: false`. Caveat: legacy manifests carry no `complete` key, so `complete is False` cannot
detect them.

**Expected test failures while the rescan runs** (they are the teeth, not a regression):
`tests/test_backtest_optimizer.py::TestAgainstRealData::test_scalp_trim_replay_keeps_bar_idx_in_range`
and `::test_trim_actually_shortens_the_frame`. Both clear once the scan rewrites the manifest with
`since=null`. Do not "fix" them. Baseline is 874 passed; these two fail only during an in-flight scan.


---

## Six UI items: limit orders, history, copilot, context strip, backtest, chart markers

Confirmed plan → implemented all six. Files: `jarvis/api/server.py`, `jarvis/api/copilot.py`,
`jarvis/api/intelligence_api.py`, `jarvis/execution/mt5_client.py`, `jarvis/data/database.py`,
`jarvis/ui/static/js/{dashboard,console,terminal}.js`,
`jarvis/ui/templates/dashboard.html`, `jarvis/ui/static/css/theme_terminal.css`,
`tools/{audit_endpoints.py,verify_dashboard_render.js}`.

**1. Backtest "does not work" — two independent defects, neither in the optimiser.**
*Parameter contract*: `dashboard.js` sent `grid_dimensions` (a list of axis names), a key
`_spec_kwargs` never whitelisted, so every run used the full 1920-geometry default and the "Search
grid" boxes were inert. Fixed by building a real `space` object in the dashboard (mirroring
`console.js:743-761`) **and** teaching the server to translate the legacy key
(`_grid_from_dimension_names`). Unit-checked across 10 request shapes; end-to-end a legacy
`grid_dimensions:["tp_r"]` run now evaluates 7 geometries against 20 for a full-space run.
*Rendering*: the report is `{modes[], series[]}` — **per trading style**, plus a coverage list. It
has no `per_symbol` and no `symbols`, and the result is nested at `payload.job.result`, not
`payload.result`. `renderBacktestResult` looked for `per_symbol` and never unwrapped `job`, so a
perfectly good run always showed "No results". Also `num(j.progress)` was rendering "—%" because
`progress` is the log list, not a percentage.

**2. Limit orders.** `place_pending_order` existed in `mt5_client` but was never exposed over HTTP,
and `modify_pending_order` did not exist at all. Added `modify_pending_order` (TRADE_ACTION_MODIFY;
None = keep the broker's current value, 0 = clear) and the routes
`POST /api/action/place_pending_order` + `/api/action/modify_pending_order`. Ticket UI gained an
order-type select (Market + 4 pending types); a pending type carries its own direction so it gets one
button. Editing loads the order into the ticket. Verified over HTTP: place, modify (TP survives an
omitted field), list, cancel, and every rejection path.

**3. Trade history.** `state.history` was assigned by nothing, so the analytics table could only ever
render its empty state. Restored the original 10 columns from `/classic`
(Ticket·Symbol·Action·Executor·Lots·Entry·SL·TP·Realized PnL·Execution time) with client-side
filters (symbol/side/executor/outcome) plus a server-side window. `/api/history` now honours
`limit`/`days`/`symbol` (all three were ignored; the cap was hardcoded 50) and keeps its bare-array
contract. Also fixed `console.js`, which read `data.trades` off that bare array.

**4. Chart markers.** `series.setMarkers()` was present in the vendored bundle and unused. Open
positions now get entry arrows from `open_time`; closed trades get exit arrows **only** where a real
close time exists. That needed a `closed_at` column (safe `ALTER TABLE` migration) because
`executed_trades.timestamp` means *entry* for an engine-logged trade and *exit* for an MT5-synced one.

**5. Context strip.** Analyst and News keep their own tabs; a `[Why|Analyst|News]` seg control beside
the ticket adds a compact read of each, rendered from the same `state` into its own containers — no
shared element id, no DOM moves, nothing renders twice.

**6. Copilot.** It could only answer about the market. Now it reads the trader's own book: open
positions with per-symbol price precision and distance to stop/target, closed-trade history, realised
P&L, working orders. Symbol detection walks the live registry + open positions instead of a
hardcoded eight. The client sends `context:{symbol,view}` so a bare "why?" means "why this chart".
Dashboard got a slide-over dock; `/classic`'s chat now sends the bearer token and surfaces HTTP
errors instead of rendering a blank bubble.

**Baselines after all six — no regressions:** pytest **883 passed / 20 deselected**;
`verify_dashboard_render.js` **88/88**; `verify_ui_layout.js` **238/238** (incl. "no unexpected
console/network errors" on all six pages); `audit_wiring.py` **135 modules, 0 broken refs**;
`audit_endpoints.py` **46/46 dispatched, 0 dead**.

**Not yet restarted.** The engine on :8501 (pid 9432, `HM_start.py paper`) predates every Python
change here. Verification ran against a separate standalone server on :8599
(`.scratch/verify_server.py`). The user must restart for the server-side fixes to take effect;
static CSS/JS/templates need only a browser refresh.

## Visual pass — three defects no assertion had caught

Reading the four screenshots (`.scratch/shots/six-*.png`) instead of trusting the green suites found
three real defects. Two of them were mine, and one of them was being actively defended by the test
suite.

**1. The copilot dock rendered open on load**, covering the order ticket. `.tt-copilot { display:
flex }` beat the UA `[hidden]` rule. Fixed with `.tt-copilot[hidden] { display: none; }`.

**2. The same `[hidden]` trap on the ticket rows — mine.** `#ticket-pending-row` and
`#ticket-edit-cancel` both carried `hidden` and both still computed `display: flex` at 44px/28px, so
the ticket showed "Place order"/"Cancel edit" *next to* the BUY/SELL pair while the type was still
Market. Fixed once for all in the reset block: `.tt-app [hidden] { display: none !important; }`.
Third occurrence in one stylesheet (dropdown, copilot, ticket) — hence the global rule rather than a
fourth per-component patch. Verified by probe: `hiddenAttr:true, display:none, h:0` for MARKET, and
the pending row correctly appearing at 44px for `BUY_LIMIT`.

**3. The history filter toolbar was clipped to 38px of its 116px** — the filters the user explicitly
asked to have restored were rendering as an unlabelled sliver of a scrollbar. Cause: the history
panel stacks two `.tt-panel__body` children and both are `flex: 1 1 auto; min-height: 0`, so the
table won the space and squashed the toolbar. Fixed with
`.tt-slot--hist > .tt-panel__body:not(.tt-panel__body--flush) { flex: 0 0 auto; }`.

**The suite said "no silently clipped content" the whole time.** That check was horizontal-only
(`scrollWidth > clientWidth`) and accepted only `overflowX: hidden|clip`, so an `overflow: auto` box
squashed vertically was structurally invisible to it. Extended to the vertical axis, with the rule
gated on *controls*: flag a box that cannot show one whole control it contains. The first attempt
(first-child height) fired on seven legitimate scroll lists — `#selection-body` at 35px over 442px
of rows — so it was narrowed until it fired on exactly the toolbar. Negative-tested by reverting the
CSS: suite goes red on exactly 3 checks (analytics at the 3 viewports), 0 false positives, then green
again with the fix. A guard that has never seen its bug pins nothing.

**4. The ticket panel is over-subscribed — also mine, from item 4.** The context strip I added inside
the ticket panel (`#ctx-body`, inline `flex:1 1 auto`) competed with the order form, which was
`flex: 0 1 auto`. Natural heights: form **526px**, pending table 144px, context 668px, against an
~834px panel. Flex cut the **form** to 252px — halving the BUY/SELL row (18px of 44 visible) and
pushing Volume / Stop loss / Take profit entirely out of view. The app's primary action, behind a
252px scroll box. Fixed by removing the inline flex from `#ctx-body`/`#reason-body`, giving every
region its natural height (`flex: 0 0 auto`) and letting the **panel** scroll as one column:
`.tt-panel--ticket { overflow-y: auto; }`. Verified: form body 526/526 (no clipping), BUY/SELL
`rowClipped: 0`.

**A measurement worth keeping:** `audit_endpoints.py` defaults to `--base http://127.0.0.1:8501`, the
live engine. The two new routes reported **DEAD** there purely because pid 9432 predates them. Same
code against `--base http://127.0.0.1:8599`: **46/46, 0 dead**. A stale process is the first
hypothesis for any "route vanished" result — do not go looking for a routing bug.

**Baselines after the visual-pass fixes:** pytest **883 passed / 20 deselected**;
`verify_ui_layout.js` **238/238**; `verify_dashboard_render.js` **88/88**;
`verify_dashboard_nav.js` **31/31**; `audit_wiring.py` **135 modules, 0 broken refs**;
`audit_endpoints.py` **46/46, 0 dead** (against a server built from the current tree).

## The copilot was transparent, and answered the wrong question

**`--hm-bg-panel` does not exist.** The copilot dock was written with it, so the declaration was
dropped as invalid, `background` computed to `rgba(0,0,0,0)`, and the order ticket's stop-loss and
take-profit fields showed straight through the chat log. `--hm-radius-md` and `--hm-text` were the
same mistake in the same block (real scale: `sm`/`—`/`lg`/`xl`/`pill`, and
`--hm-text-primary|secondary|dim`). Measured with a geometry probe: `background: rgb(26,36,56)`
after, `rgba(0,0,0,0)` before. **A `var()` naming a missing token throws nothing and appears in no
console** — the whole declaration vanishes.

New permanent guard: **`tools/audit_css_tokens.py`** — every `var(--x)` must resolve to a
definition, `var(--x, fallback)` reported separately as a smell rather than a failure. 172 tokens
defined, 0 undefined now; negative-tested (exit 1 with a bad token, exit 0 clean).

**Intent routing: "how are my trades doing?" answered with a single instrument.** `_POSITION_WORDS`
contains `"my trade"`, which greedily matched, so a book-level performance question got
"you have no open position on XAUUSD". Fixed with `_PERF_PHRASES` checked first **when no symbol was
named** — the discriminator is whether the trader named an instrument: "how is my EURUSD doing?" is
about that position, "how are my trades doing?" is about the book.

**A comment claimed behaviour the code never had.** The block comment above `_POSITION_WORDS` said
it existed so that *"how is my EURUSD doing"* reaches the position handler — but no word in that list
appears in the sentence, so it fell through to the help text. So did *"how is my symbol doing?"*,
**which the help text itself suggests as an example**. Added `_POSITION_PHRASES`
(`"how is my"`, `"how's my"`, …) gated on an instrument being known (named, or the on-screen focus).
Verified: 10 phrasings route correctly, including `"how's my gold doing?"` → XAUUSD via the alias.

**The dock does overlap the ticket, and that is now stated rather than denied.** A 400×560 panel
anchored bottom-right lands at (1184, 398) while the ticket occupies (1258, 129, 330×834) — its top
edge cuts through the BUY/SELL row. The old comment claimed the panel "never blocks a click on the
ticket", which is false. Left as a deliberate drawer trade-off, but the comment now says so.

## Committed and pushed

`6edd231` — 18 files, +2342/−116, on `main`, pushed to `github.com/TechRhino1/HM-AI`.
Bundle backup written first (`git bundle create .git/backup/repo-<ts>.bundle --all`).

**The push needed the `helper-selector` bypass again, and this time the plain push never completed at
all** — a background `git push origin main` sat at a 0-byte log for **6m37s** before being killed.
The documented bypass (`git -c credential.helper= -c credential.helper="!$GCM"`) finished in **23s**,
`exit=0`. Do not wait on a plain push here; go straight to the bypass. Verified with
`git ls-remote` (remote `6edd231` == local HEAD) rather than the push message. `diagnose-git-push-auth`
updated with this: the stall is unbounded, not ~40s.

## Closing the last verification gap: the backtest renderer had no test

The backtest fix from `6edd231` was verified in two halves — the parameter contract (`_space_from_spec`)
and the renderer's shape handling — but **nothing ever fed `renderBacktestResult` a real report**.
`tools/verify_dashboard_render.js` answered every `/api/backtest/` request with `{status:'OK', jobs:[]}`,
so the suite stayed 88/88 green through the entire period the page was broken. "The renderer was never
called" and "the renderer works" look identical from outside. That is the same class of blind spot that
produced the original complaint, so it was the right place to spend the remaining effort.

**What was added** (266 lines to the render suite, no new files): fixtures for the real report shape
(`{modes[], series[]}`, built per *trading style* at `optimizer.py:888-995`, nested at
`payload.job.result`), a meta payload, and two jobs — one with a real report, one whose result body is
empty. The view is entered through its real wiring: the `6` shortcut, then a click on `tr[data-job]`,
which exercises `loadJobs()`, the delegated click handler, `pollJob()`'s DONE branch and
`loadJobResult()`'s fetch. 24 new checks, including a control (`preFixReportReader`) that runs the
pre-fix lookup against the same payload and must find nothing.

**Negative-tested, both directions.** Reverting `unwrapReport` to its pre-fix form turns **16** checks
red, with the user's own words in the failure detail: `No results in this job`. Reverting the number
grouping turns exactly **1** red. A suite that has never seen the bug pins nothing.

**One real defect found by reading the renderer, not by the suite.** The per-style table rendered trade
counts raw (`1204`) while every neighbouring numeric column was formatted (`8,731`) — inconsistent
within a single row. Changed to `num(..., 0)`; that is the entire 2-line diff to `dashboard.js`.

**Two harness gaps surfaced as if they were app bugs, and both are worth remembering.** The DOM stub
had no `<select>.options`, so `loadBacktestMeta`'s `objSel.options.length` threw and killed the run with
a `dashboard.js:3891` stack. And adding a phase that switched views silently disabled the news
calendar's per-second tick (`tickNews()` early-returns unless `state.view === 'news'`), turning 5
clock-advance checks red — the code under test was fine. Fixed by giving the stub a tree-derived
`options` getter and by handing the view back to the calendar before the advance.

## Baselines after the change (each suite run alone, scratch server on :8599)

`pytest -q` **883 passed / 20 deselected** · `verify_dashboard_render.js` **111/111** (was 88) ·
`verify_ui_layout.js` **238/238** · `verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **44/44** ·
`audit_wiring.py` **135 modules, 0 broken refs** · `audit_endpoints.py --base :8599` **46/46, 0 dead** ·
`audit_css_tokens.py` **172 tokens, 0 undefined**. Nothing regressed; the only rise is the render
suite, from the 23 new checks.

**`nohup … &` does not survive the tool call.** A scratch server launched that way answered
`health=200`, then the next call got `ERR_CONNECTION_REFUSED` on every route while the log ended on a
normal startup line. `run_in_background: true` is the only thing that keeps it alive. `verify_ui_live.py`
binds :8599 itself, so that port must be freed first.

## The same coverage gap, one panel over: the trade history had no test either

Applying §3d to the other five items found the history panel — the feature the user explicitly asked
to restore — was **also** untested. `verify_dashboard_render.js` had no `/api/history` branch at all,
so the stub fell through to `{}` and `renderHistory()` rendered zero rows. Nothing asserted the ten
columns, the four filters, the summary line, or the `closed_at` disambiguation. 19 checks added.

**Two real defects, both in the restored feature.**

1. **The Window filter reached only half the data.** `/api/history` merges two sources — journal rows
   from `TRADE_DB.fetch_recent_trades()` and MT5 out-deals — but `days` was passed only to
   `history_deals_get`. `fetch_recent_trades(limit=100)` had no `days` parameter at all, so every
   journal row ignored the window. Measured before the fix: `?days=1` returned **185 rows whose oldest
   was 24 days old**, 117 of them outside the window. After: 36 rows, oldest 0 days, **0 outside** —
   and 0 outside at 7d and 60d too. Fixed by adding `days=None` to `fetch_recent_trades` (default None
   preserves the classic-terminal and console callers, which pass only a limit) and threading the
   route's value through. 4 pytest tests, negative-tested: reverting the parameter turns exactly the
   2 window tests red with `4 != 1`, while the 2 legacy-contract tests correctly still pass.

2. **The time column was mislabelled, and I had introduced that.** The original table (38d9f00) was a
   *closed*-trade list whose first column was literally `Closed`, rendering `t.time || t.close_time`.
   When I restored the panel I renamed it "Execution time" and rendered `t.timestamp` — which is the
   **entry** time for a journal row and the **exit** time for a synced MT5 deal, so one unlabelled
   column carried two meanings. The server already emits `closed_at` precisely to disambiguate (the
   chart's exit markers use it), and `renderHistory` ignored it. Now shows `closed_at` where it exists,
   marks rows without one `(open)`, and the header says `Closed` again. Negative-tested: reverting the
   display turns exactly 2 checks red ("rendered the entry time"); reverting the header turns exactly 1
   red ("header drift").

**The lesson worth keeping:** when restoring a feature from git history, diff the *label* against the
data, not just the column list. `git show <old>:<file>` gave the original header in one command and
showed the drift immediately — the column count matched, so nothing looked wrong.

## Baselines (each suite alone, scratch server on :8599)

`pytest -q` **887 passed / 20 deselected** (883 + the 4 new) · `verify_dashboard_render.js` **130/130**
(was 111) · `verify_ui_layout.js` **238/238** · `verify_dashboard_nav.js` **31/31** ·
`verify_ui_live.py` **44/44** · `audit_wiring.py` 135 modules 0 broken · `audit_endpoints.py` 46/46 ·
`audit_css_tokens.py` 172/0. No regressions.

## Third instance, and the worst one: the copilot had no tests at all

The sweep I documented as a repeatable step (`grep` the controller's endpoints, `grep` the stub's
branches, any path with no branch is a renderer with no test) found three more untested surfaces —
copilot, auto-selection, regime-policy — and one of them was not a frontend gap at all.

**`jarvis/api/copilot.py` — 391 lines of intent dispatch, zero tests.** Both bugs it has ever had were
found by hand-probing a live session, so nothing would have caught a regression. It is also the one
module whose stated contract is that it never invents a number, which makes silent wrong-but-plausible
answers its characteristic failure mode: *"how are my trades doing?"* was answered with *"you have no
open position on XAUUSD"* — true, and not what was asked.

`tests/test_copilot_intents.py` — 27 tests, no server and no live DB (the journal is patched):

* **Routing**, asserted by a marker string only the intended handler emits, so a routing change shows
  up as the wrong marker rather than as a similar-looking reply.
* **The help text is a promise about the router.** It extracts every `*'…'*` example from the help
  reply and asserts each one reaches a handler. This is not hypothetical: the help suggested *"how is
  my symbol doing?"* while that sentence fell through to the help text. The test keeps the two in step.
* **Both historical bugs as regressions**, including the discriminator — a *named* instrument routes to
  that position, while the on-screen **focus alone must not** flip a book-level question back to a
  single instrument.
* **Honest mode**: no account → "will not guess the balance"; no broker client → "no broker client is
  attached" rather than the very different "you have no working orders"; unreachable journal → "not
  reachable"; unreadable broker → "could not read working orders".
* **Per-symbol precision**, from the registry: XAUUSD 2, USDJPY 3, EURUSD 5.

**One defect found by writing them.** `_performance_answer` said *"Realised over the last N closed
trade(s)"*, where N counted only trades with a **non-zero** result — so a break-even trade made it say
"1 closed trade" while the history answer listed two rows for the same journal. The count was a claim
about the journal that the journal contradicted. Fixed to "Realised on N trade(s) with a recorded
result", which is what it actually counted. (Left the win-rate denominator alone: excluding break-even
from `wins/(wins+losses)` is defensible, and changing it would alter a number the user has seen.)

Negative-tested: reverting both intent fixes turns **5** routing tests red, reproducing the original
bug verbatim (`how are my trades doing?` → `**Open positions — 1**`).

Also added two probes to `verify_ui_live.py` for `/api/copilot/ask`, which had none: the route answers,
and a book-level question is answered about the book. 44 → 46.

## Baselines (each suite alone, scratch server on :8599)

`pytest -q` **914 passed / 20 deselected** (887 + 27) · `verify_dashboard_render.js` **130/130** ·
`verify_ui_layout.js` **238/238** · `verify_dashboard_nav.js` **31/31** · `verify_ui_live.py`
**46/46** (was 44) · `audit_wiring.py` 135 modules 0 broken · `audit_endpoints.py` 46/46 ·
`audit_css_tokens.py` 172/0. No regressions.

## Round 4 — auto-selection and regime-policy coverage (render 130 → 153)

The sweep continued to the two Analytics panels, neither of which had a fixture. Added
`SELECTION_PAYLOAD` (4 decisions, 2 `is_tradeable`), `SELECTION_UNAVAILABLE` (status UNAVAILABLE, the
503 body), and `REGIME_POLICY_PAYLOAD` (SWING 2 regimes, SCALP 1, POSITION `{error}`), plus the
ability for `fetchStub` to answer **non-200** — previously impossible, which is why **every panel's
error branch was unreachable** and the `status`/`ok` fields of the stub response were never exercised.
22 new checks: 11 auto-selection, 11 regime policy.

Auto-selection is driven through the **watchlist refresh button** (the control a user presses when a
panel looks stale) rather than by calling `loadSelection()`, and the 503 branch by flipping
`selectionAvailable`. Regime policy has **no boot path at all** — it loads from `setView('analytics')`
— so the `5` shortcut is the only way in; that is now the only thing driving it.

**The failing check was mine, not the app's.** `the metrics count modes including the one that errored`
used `/Modes<\/span><span class="tt-metric__value">3</.test(regMetrics.replace(/\s+/g, ''))`. Stripping
whitespace also removes the space in `<span class="…">`, so the pattern could never match — while the
printed detail showed the *correct* markup, which is what made it look like a rendering bug. Replaced
with a `metricValue(html, label)` helper that binds each value to its own label. That also **closed a
real hole**: the adjacent check used `indexOf('>3<') && indexOf('>2<')`, which passes a swap of two
counters despite its comment claiming a swap "shows up". Negative-tested: swapping
`Conditions`/`Tradeable` in the renderer turns exactly **1** check red (`Conditions=2 Tradeable=3`);
under the loose form it turned **0**. Added a fourth check for `Own geometry` (was unasserted).

Also fixed a harness collision: `verify_ui_live.py` starts its **own** server on :8599, so running it
while `.scratch/verify_server.py` held the port made its probes hit the engine-less scratch server —
`attached=False`, `503 UNAVAILABLE`, reading exactly like a product regression. With the port free:
**46/46**, `attached=True`, `decisions=1`. Both traps written into `TRAPS.md`.

### Final baselines — all eight suites, this tree

`pytest -q` **914 passed / 20 deselected** · `verify_dashboard_render.js` **153/153** (was 130) ·
`verify_ui_layout.js` **238/238** · `verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **46/46** ·
`audit_wiring.py` 135 modules 0 broken · `audit_endpoints.py` 46/46 0 dead · `audit_css_tokens.py`
172 tokens 0 undefined. Diff for the whole round is `tools/verify_dashboard_render.js` alone
(+277/−4) — **no production file changed**, which is what makes "no regressions" a structural claim
rather than a hopeful one.

## Round 5 — the order path: a false confirmation of a trade (production fix)

Ran the coverage sweep mechanically instead of declaring it done, and it was **not** exhausted: seven
endpoints the controller calls had no stub branch, and they were all one class — **POST actions whose
response drives rendering**. Every one fell through to `200 {}`, so each success path rendered against
an empty body and each failure path was unreachable.

Following that class found a live defect. **A refused order is answered with HTTP 200.** `mt5_client`
puts the outcome in the body — `FAILED` when the broker said no, `BLOCKED` when the mode gate refuses —
and the route passes the dict straight through (`_send_json(res)`, default 200). Measured against the
real server:

```
POST /api/action/manual_trade   {BUY, sl above the fill}
{"status": "FAILED", "reason": "BUY stop-loss 2100.0 is not below the fill price 2000.0"}   HTTP=200
```

- `manual_trade` guarded on **`if (res.ok)`** alone → a **rejected market order was announced as
  "BUY 0.2 XAUUSD submitted"**, green toast and all. Reproduced verbatim by the harness.
- The three pending-order handlers tested `status !== 'FAILED'`, so a **`BLOCKED`** order — never sent —
  was announced as **"placed"**.

Fixed with one shared predicate over `{FAILED, BLOCKED, REJECTED, ERROR}` plus a shared message chain
so `reason` is not dropped for "Order rejected: HTTP 200". Deliberately a **deny-list of refusals, not
an allow-list of successes**: a status the list has not heard of can then never turn a real success into
a false failure, while every known refusal is caught. (An allow-list would be stricter still; flagged to
the user as the remaining judgement call rather than taken unilaterally on a money path.)

`BLOCKED` is **latent, not live**: `init_connection` accepts `"backtest"`/`"offline"` as healthy
simulated modes while the action gates only allow `live/demo/paper`, so such a client reports itself
connected and refuses every order — but no call site passes those modes today. Fixed anyway because it
is one keyword argument from being live.

Coverage: harness can now answer **POST** (recording url + body), the `El` stub's `value` defaults to
`''` as a real input does, and 10 new checks drive both orders through the real buttons and read the
toasts. New `tests/test_action_response_contract.py` (6 tests) drives the real `do_POST` body against a
real paper `MT5Client`, and pins the frontend/backend contract cross-language: it reads the refusal
statuses out of `mt5_client.py`, reads the UI's set out of `dashboard.js`, and fails if the backend can
refuse with a status the UI has never heard of.

Negative-tested, each half separately and disjointly: reverting the market guard → exactly **2** red
(`BUY 0.2 XAUUSD submitted`); reverting the pending guard → exactly **2** red, the other pair;
dropping `BLOCKED` from the UI set → the contract test red with
`the broker can refuse with ['BLOCKED','FAILED'] but the UI only knows ['ERROR','FAILED','REJECTED']`.

One harness-side false failure again: the stub's `value` was `undefined` where a real input reads `''`,
so `slEl.value !== ''` passed on an empty field and the test posted `NaN`. The stub was wrong, not the
app — same family as the missing `.options`. Written into `TRAPS.md`.

### Baselines after round 5

`pytest -q` **920 passed / 20 deselected** (914 + 6) · `verify_dashboard_render.js` **163/163** (was 153) ·
`verify_ui_layout.js` **238/238** · `verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **46/46**
(port free) · `audit_wiring.py` 135 modules 0 broken · `audit_endpoints.py` 46/46 0 dead ·
`audit_css_tokens.py` 172 tokens 0 undefined. No regressions.

## Round 6 — sweeping the class, not the instance

Having found one `res.ok`-only guard, the disciplined move was to sweep the **class**. `grep -nE "res\.ok"`
over `dashboard.js` returned exactly five guards, four already fixed; **one remained**: `cancelJob`.

`/api/backtest/cancel` (`intelligence_api._post_cancel`) answers **200** with
`{"status": "NOOP", "cancelled": false}` when the job had already finished — nothing was cancelled — so
`if (res.ok) toast('Cancel requested')` claimed a cancellation that did not happen. Not a money path, so
far less severe than round 5, but not harmless either: the module docstring says jobs are serialised
because *"this machine has under a gigabyte free"*, so a user told "cancel requested" stops watching a
job that is still holding memory. Fixed to branch on `data.cancelled`, with a distinct
"That job is no longer running" for the NOOP.

**The contrast is the useful part.** `runJob` (`/api/backtest/run`) was already correct — it checks
`!res.ok || !res.data` *and* requires a `job_id` before claiming success. So the file's own convention
is to validate the payload; `cancelJob` and `manual_trade` were the outliers, not the norm. Worth
saying plainly rather than implying the whole controller was careless.

Coverage: 5 new harness checks (the cancel POST body, and all three outcomes — requested / no-longer-
running / error) and 2 new tests in `test_action_response_contract.py` pinning the route contract that
forced the UI to read `cancelled`. Negative-tested: reverting `cancelJob` to `res.ok` turns exactly
**2** red, the detail showing `{"text":"Cancel requested","cls":"tt-toast tt-toast--ok"}`.

### Baselines after round 6

`pytest -q` **922 passed / 20 deselected** (920 + 2) · `verify_dashboard_render.js` **168/168** (was 163) ·
`verify_ui_layout.js` **238/238** · `verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **46/46**
(port free) · `audit_wiring.py` 135 modules 0 broken · `audit_endpoints.py` 46/46 0 dead ·
`audit_css_tokens.py` 172 tokens 0 undefined. No regressions.

## Round 7 — the copilot chat panel, and an XSS in its other front end

The last un-stubbed endpoint was `/api/copilot/ask`, so the chat panel's rendering was the remaining
gap. Round 3 had covered the *server* side (27 tests); the frontend had none. Two defects, both found by
writing the checks.

**1. Bold inside a bullet rendered as literal asterisks (dashboard).** `copilotHtml`'s bullet branch
`return`ed early, so the inline pass never ran on a bullet line — while nearly every bullet the copilot
writes wraps its label in `**` (`- **Current Bias**: …`, `- Balance **1,234.56**`). So most of a typical
answer reached the reader as `• **Current Bias**: BULL`. Fixed by extracting the inline pass into
`copilotInline()` and applying it to the bullet body too.

**2. An XSS in the classic terminal's copilot (`terminal.js`).** Its `bubble()` helper does
`b.innerHTML = html`, and the answer was interpolated raw:

```js
bubble(`🤖 <b>HM Algo 2.0:</b><br>${data.response || "No response."}`);   // no escaping
```

The dashboard's sibling escapes first and says why in a comment (*"so a symbol name or a broker comment
containing markup can never inject HTML"*). The terminal's answers interpolate **broker and journal
strings** — symbol names, deal comments, order types (`- **#{p.ticket} {sym_p} {p.type}** …`). The query
itself is **not** echoed (checked: `copilot.py` uses it only for routing), so the surface is
broker/journal-controlled data, not remote input — but it is exactly the data the dashboard deemed
untrusted. Fixed: `escapeHtml()` added and applied to the answer, the refusal reason and the transport
error, with the same markdown-lite so the terminal also stops showing raw `**`.

**OPEN FINDING, not fixed — `terminal.js` has no escaping convention anywhere.** The copilot bubble was
one of a pattern: server data is interpolated into `innerHTML` at the pending-orders table (1139),
positions (1874), history rows (1078/1119), the radar list (1573), the news feed (1658), the decision
rationale (2091/2108/2124) and the market-status line (1474). Fixing all of those is a substantial change
to the classic terminal's rendering and should be a deliberate decision, not a side effect of a chat-panel
task. Flagged to the user with the site list.

New tool `tools/verify_copilot_render.js` (23 checks): extracts each file's `escapeHtml` / `copilotInline`
/ `copilotHtml`, **evaluates** them, and asserts hostile input is escaped, bullets+bold render, and — the
point of it — that **the two implementations produce byte-identical output** across nine cases, so the
duplication cannot silently diverge again. Plus call-site checks, because a function-level test cannot see
whether the bubble actually calls the helper. Negative-tested: reverting the answer to raw interpolation
→ 2 red quoting the line; removing the escaping → 3 red, the cross-renderer diff showing dashboard
`&lt;img…` against terminal `<img src=x onerror=alert(1)>`.

Two assertion bugs of mine along the way, both the same shape as before: `cpHtml` read the **whole log**
(so a check could pass on the boot greeting rather than the answer) and asserted a **className** through
`deepHtml`, which only serialises `innerHTML`. Scoped the captures to the new bubbles.

### Baselines after round 7

`pytest -q` **922 passed / 20 deselected** · `verify_dashboard_render.js` **183/183** (was 168) ·
`verify_copilot_render.js` **23/23** (new) · `verify_ui_layout.js` **238/238** ·
`verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **46/46** · `audit_wiring.py` 135 modules
0 broken · `audit_endpoints.py` 46/46 0 dead · `audit_css_tokens.py` 172 tokens 0 undefined.

### Round 7 pushed and re-verified

`81f72a5` pushed (`6dc113a..81f72a5 main -> main`), confirmed **independently** with `git ls-remote`
(`refs/heads/main` = `81f72a5dfc0675e0f1ff5b3a8bc9872a4befff70` = local HEAD) — the push message alone
is not proof. The whole battery was then **re-run from scratch on the pushed tree** (not remembered):
every number above reproduced exactly. Tree clean, scratch server on :8599 stopped, no listeners left.

Coverage sweep re-run and now **clean**: 23 controller paths vs 21 stub branch prefixes, normalising
away query strings and matching by prefix → **0 uncovered**. (The first sweep reported 9 "uncovered"
and all 9 were my own artefact: it compared full call strings incl. `?symbol=…` against branch
prefixes. Normalise the path before comparing, or the sweep invents gaps.)

**Open finding sharpened.** The `terminal.js` non-escaping is worse than text injection at two sites:
`p.symbol` is interpolated into an **`onclick` attribute** — line 1149 (pending orders) and 1894
(positions), `onclick="window.setSymbol('${p.symbol}')"` — so a quote in the symbol breaks out of the
attribute rather than merely rendering markup. The pending-orders table also prints the broker-supplied
order `comment` raw (1157). Still reported, not fixed: it is a deliberate decision about the classic
terminal's rendering, not a side effect of a chat-panel task.

## Round 8 — a render harness for the classic terminal (53 checks), and the findings it produced

`jarvis/ui/static/js/terminal.js` is ~3,400 lines and had **zero renderer coverage**. The dashboard had
`verify_dashboard_render.js`; the terminal had only `verify_copilot_render.js` (copilot helpers in
isolation) and a `verify_ui_live.py` check that index.html *references* the file. Nothing proved a single
table row was ever built — which is how the `closed_at` fix landed in one front end and not the other.

### The shared DOM stub

Extracted the stubbed DOM out of the dashboard harness into **`tools/lib/dom_stub.js`**
(`createDom({templateIds, docRoots, templateTree})`). Both harnesses now use it. A copy per harness would
drift, and the drift would be invisible — a harness whose stub is subtly weaker still prints PASS, which
is the same failure mode as a duplicated production renderer. The dashboard harness was refactored onto
it and **still passes 183/183**, which is what makes the refactor safe rather than hopeful.

Two fidelity additions the terminal forced: **`innerText`** (aliased to `_text` — the terminal writes
`history-count` through it, and without the alias the write became a plain property `deepHtml` never saw,
so the count read blank) and **`document.activeElement`** (the terminal guards input writes with
`activeElement !== inputEl`). `dashboard.js` uses neither, so both are additive.

### Fixtures came from the server, not from reading the renderer

Captured the real shapes from a running instance: `/api/history` is a **bare array** (row keys `action,
closed_at, entry_price, executor, id, realized_pnl, regime, sl, symbol, ticket, timestamp, tp, volume`);
`/api/pending_orders` a bare array; `/api/radar` → `{opportunities:[…]}`; `/api/news` → `{news:[…]}`;
`/api/telemetry_state` → `account{balance,equity,free_margin,login,margin_level,server,…}`. `positions`
was empty live, so that fixture uses the fields `renderActiveTradesDOM` actually reads.

### A fetcher registered inside `setInterval` is invisible to an inert stub

The boot handler fetches telemetry, radar, news and candles **directly**, but registers history only as
`setInterval(fetchHistory, 5000)`. With `setInterval: () => 0` the history table was never populated and
all twelve history assertions were no-ops. Fixed by **capturing** the intervals and ticking each once
after the boot drain — and the harness now pins the behaviour as a pair of checks ("the boot handler
defers history to an interval" + "ticking the intervals brings it in"). The user-visible consequence is
real: the history table is empty for up to 5s on first paint.

### Findings (measured, not asserted from reading)

* **The terminal never reads `closed_at`.** Its history column renders `timestamp`, which is the ENTRY
  time for a journal row and the EXIT time for an MT5-synced one — the exact ambiguity fixed in the
  dashboard (`t.closed_at || t.timestamp`, titled "Closed" vs "Opened; not yet closed"). A cross-file
  guard now asserts dashboard-has-it / terminal-does-not, so the divergence cannot be forgotten.
* **16 of 166 controller ids are absent from `index.html`**: 8 `acc-*` that are **never read at all**
  (dead `el` entries) and 8 `desk-banner-*` whose writes are all `if (el.deskBannerX)`-guarded, so they
  silently do nothing. The desk was refactored to a synthesis card using `plan-*`/`payoff-*`, which do
  exist and do carry the values — so this is dead code from a refactor, **not** a functional gap.
* **The injection is now measured, not theorised.** Running the real renderers with
  `symbol = "XAUUSD');alert(1);//"` yields, verbatim:
  `onclick="window.setSymbol('XAUUSD');alert(1);//')"` — the quote closes the JS string and the rest
  executes. Same breakout in the positions row. A broker `comment` of `<img src=x onerror=alert(1)>`
  reaches `innerHTML` raw. Recorded as **FIND/CLEAR** lines rather than PASS/FAIL, so "53 checks passed"
  stays honest: a FIND is a reproduced defect that was reported, not an approved behaviour.

### Round 8 baselines

`pytest -q` **922 passed / 20 deselected** · `verify_dashboard_render.js` **183/183** (after the refactor) ·
`verify_copilot_render.js` **23/23** · `verify_terminal_render.js` **53/53 (new)** + 5 findings.

## Round 9 — the terminal's escaping, fixed (the round-8 finding, now closed)

Round 8 measured the injection but reported it rather than fixing it, because rewriting eight-plus render
sites in the classic terminal is not a side effect of adding a harness. With the reproduction and the
harness in hand it became a deliberate task, so it was done here.

### Why escaping was safe to apply (checked before touching anything)

Escaping only changes `< > & " '`. The only way it can break a display is if a payload field *intends*
to carry markup. Checked: no field does — `jarvis/market/news.py:337-357` builds `status_badge` from
emoji plus text (`f"🔴 LIVE NOW (Ends in {rem_mins}m)"`), and a grep for tags in strings assigned to
`status_badge` / `direction_bias` / `impact_analysis` / `description` returns nothing. For a benign
payload the rendered output is **byte-identical** — which the harness confirms, since all 53 checks still
pass unchanged.

### The non-obvious part: entity escaping does NOT fix an inline handler

`escapeHtml()` escapes `'` to `&#39;`, which looks like it protects
`onclick="window.setSymbol('<symbol>')"`. It does not: the **HTML parser decodes entities before the JS
is parsed**, so `&#39;` reaches the JS layer as a plain `'` and closes the string anyway. A value inside
an inline handler sits in a JS string inside an HTML attribute, so it must be escaped for the **JS layer
first, then the attribute layer** — `escAttr()` does
`escapeHtml(s.replace(/\\/g,"\\\\").replace(/'/g,"\\'"))`, giving `\'` which survives entity decoding as
an escaped quote. For a well-formed symbol (`[A-Za-z0-9._#-]`) the output is byte-identical to before.

### Applied

`escAttr()` for the three inline-handler sites (pending orders, positions, radar cards) and
`escapeHtml()` for every server string reaching `innerHTML`: pending symbol/type/comment, positions
symbol/type, history symbol/side/timestamp, radar symbol/strategy/next-open, news
event/currency/impact/status/actual/forecast/previous/affected-pairs, the news detail pairs, market-status
next-open, decision-rationale waiting/failing reasons, invalidation levels and threat vectors.
**3 → 40 `escapeHtml` call sites.**

Verified by sweep, not by eye: a script that lists every `${…}` inside an `.innerHTML` assignment and
excludes those already wrapped in `escapeHtml(`/`escAttr(`/`Number(`/`formatPrice(` reports **0 remaining**
(it reported 5 after the first pass — the news detail pairs, the invalidation levels and the threat
vectors). The first, unfiltered sweep reported 77 and was useless: most hits were `textContent`,
`alert()` and `fetch()` template strings, none of which parse HTML.

### Two measurement bugs in my own harness, both producing a wrong answer first

* **`deepHtml()` cannot distinguish a parsed tag from text that looks like one.** It concatenates each
  node's decoded `_text`, so a properly escaped `&lt;img …&gt;` reappears as `<img …>` and the check
  still reports a finding. The security property is "does the **markup parser** see a tag", and only the
  **raw `innerHTML` string** answers that. Switched the checks to read `innerHTML`.
* **Searching for the payload text is not the same as testing the escape.** After the fix the attribute
  still contains `');alert(1);//` — preceded by a backslash, which is exactly what makes it safe. The
  check now counts **unescaped** quotes (`replace(/\\'/g,'')` then expect exactly 2, the delimiters).

Both flips are the point of the harness: FIND → CLEAR is the evidence the fix works, and a check that
cannot tell the difference would have reported a fix that did not happen.

### Round 9 baselines — full battery, all green

`pytest -q` **922 passed / 20 deselected** · `verify_terminal_render.js` **53/53**, 5 findings now
**CLEAR** · `verify_dashboard_render.js` **183/183** · `verify_copilot_render.js` **23/23** ·
`verify_ui_layout.js` **238/238** — and its `forex` page **is** `index.html`, so the terminal changes are
browser-verified by that page's "no unexpected console/network errors" check ·
`verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **46/46** · `audit_endpoints.py` 46/46 0 dead ·
`audit_wiring.py` 0 broken refs · `audit_css_tokens.py` 172 tokens 0 undefined.

## Round 10 — the terminal's history column: "Execution Time" was false for half the rows

Round 8 flagged the terminal's `closed_at` divergence and round 9 left it as a product call ("which
instant should that column show?"). **Reading the header settled it — it is not a product call.** The
column is headed **"Execution Time"** (`index.html:598`) and renders `t.timestamp`, which is the *entry*
time for a row the engine logged and the *exit* time for one synced from a closed MT5 deal. So the header
is false for half the rows, which is precisely the defect already diagnosed and fixed in the dashboard
(`dashboard.js:3668-3682`, header "Closed", `closed_at || timestamp`, cell titled "Closed" vs
"Opened; not yet closed"). An established fix for the same payload in the other front end beats a
preference — the earlier framing was wrong, and checking the header is what showed it.

Applied the same rule to `renderHistoryDOM`: prefer `closed_at`, fall back to `timestamp` for rows that
have none, mark those with an `(open)` suffix and a `title` of "Opened; not yet closed". The header in
`index.html` now reads **"Closed"**, matching the dashboard.

The harness check flipped from pinning the bug to pinning the fix, and the cross-file divergence guard
flipped from "dashboard only" to **"both front ends read `closed_at`"** — a guard that had one round of
life as a defect marker and now guards the pair. 53 → **55 checks**.

### Round 10 baselines — full battery, all green

`pytest -q` **922 passed / 20 deselected** · `verify_terminal_render.js` **55/55**, 5 findings CLEAR ·
`verify_dashboard_render.js` **183/183** · `verify_copilot_render.js` **23/23** ·
`verify_ui_layout.js` **238/238, 0 FAIL** (`forex` = `index.html`, so the header and cell changes are
browser-verified) · `verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **46/46** ·
`audit_endpoints.py` 46/46 0 dead · `audit_wiring.py` 0 broken refs · `audit_css_tokens.py` 172/0.

Note on reading these logs: `grep -c FAIL <log>` exits **1** when it finds none, so a
`suite > log; grep -c FAIL log` chain reports the *task* as failed on a perfectly green run. Read the
suite's own total line, not the wrapper's exit code.

## Round 11 — the guard that validated the set it had already filtered through

`tests/test_action_response_contract.py` had a cross-language drift guard with a comment promising that
"a new one shows up". It could not show up. It read the status literals out of `mt5_client.py` and then
filtered them through the very literal it was validating::

    emitted  = set(re.findall(..., mt5_client.py))
    refusals = {s for s in emitted if s in {"FAILED", "BLOCKED", "REJECTED", "ERROR"}}
    assertTrue(refusals <= ui_refusal_set)

`refusals` is a subset of `{"FAILED","BLOCKED","REJECTED","ERROR"}` **by construction**, so the only
assertion left was `{"FAILED","BLOCKED"} <= ui_set` — a statement about two hard-coded names. A brand-new
refusal status added to the broker was filtered out *before* the assertion and could never fail the test.
The comment described the intent; the code did the opposite of it.

**Proved it, rather than reasoned about it.** Mutation test: appended `_MUT = {"status":
"PARTIALLY_REJECTED"}` to `mt5_client.py` and ran the file. Replaying the old guard's logic on that same
vocabulary gives `refusals = ['BLOCKED','FAILED']`, `refusals <= ui -> True` — **passes**, on a status the
UI has never heard of. The new guard reports `unclassified = ['PARTIALLY_REJECTED']` and fails. Restored
the file inside a `finally` and asserted byte-identity afterwards (`RESTORED bytes equal: True`).

The replacement is a set of **exhaustive classifications** — `REFUSALS` / `COMPLETIONS` / `NON_2XX_ONLY` —
plus `test_every_status_the_action_path_can_emit_is_classified`, which goes red on any unclassified status
and so forces a human decision. That is the only shape a guard like this earns its place in: it must be
able to fail on the thing it claims to catch.

### Why a deny-list is sound here (the open question from round 8)

Scoped the enumeration to the `/api/action/` dispatch inside `do_POST` rather than the whole of
`server.py`: the status endpoint emits `SAFE_MODE` / `OPERATIONAL` about the *system*, a different
vocabulary from "did your order happen", and letting those in would make the classification meaningless.
The action path's 200-with-a-refusal set is **closed at `FAILED` and `BLOCKED`** — the route hands the
broker's dict to `_send_json(res)`, default 200. Everything else the server refuses with carries a non-2xx
code (`UNAUTHORIZED` 401, `FORBIDDEN` 403, `BAD_REQUEST` 400, the 500 path), so `res.ok` rejects it before
the body is read. That is what makes a body-level deny-list sufficient rather than sloppy.

### Round 11 baselines — full battery, all green

`pytest -q` **924 passed / 20 deselected** (was 922; the file went 8 → 10 tests) ·
`verify_terminal_render.js` **55/55**, 5 findings CLEAR · `verify_dashboard_render.js` **183/183** ·
`verify_copilot_render.js` **23/23** · `verify_ui_layout.js` **238/238, 0 FAIL** ·
`verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **46/46** · `audit_endpoints.py` 46/46 0 dead ·
`audit_wiring.py` 135 modules 0 broken refs · `audit_css_tokens.py` 172 tokens 0 undefined.

Correction to the round-9 note: the terminal has **41** `escapeHtml()` *call sites*, not 40 — 42
occurrences of `escapeHtml(` minus the definition on line 67. The suite's counter agrees; the note was one
short, most likely taken before the round-10 history-cell edit added the last one.

Scratch server for the browser suites is now `.scratch/srv8611.py` on **8611**, not 8599:
`tools/verify_ui_live.py` starts its own server on 8599 (`PORT = 8599`), so anything already listening
there makes its probes hit the engine-less server and read as `attached=False` / `503`.

**Swept the class, not the instance.** Regex for the tautology shape
(`{x for x in SRC if x in {literal}}`) across all of `tests/` returns exactly **1** hit — line 137 of
`test_action_response_contract.py`, which is inside the docstring quoting the old broken code. Zero live
guards of this shape remain.

**Committed `11996d2`** ("test(contract): rebuild the refusal drift guard so it can actually fail"),
pushed with the GCM bypass, verified `git ls-remote origin refs/heads/main` = `11996d212ddf5765a14d3d6f9bed3a41a09f2244`
= local HEAD, push log `5d8f594..11996d2 main -> main EXIT=0`. Bundle: `.git/backup/repo-20260917-234824.bundle`
(3,332,238 bytes). **No product code changed in this round** — the tree carried only the test file plus the
two memory files.

## Round 12 — the copilot's conversation memory and the optional provider layer

Task #6 (the bot chat) was the last open item, with three sub-goals left undone: session conversation
memory, the optional provider layer, and (already done) the `Authorization` header and the dashboard
surface. The plan in `docs/PLAN-2026-09-17-six-ui-items.md` had offered A/B/C for this item and
recommended **C** — rule-based as the always-available floor, a real model behind config — which is what
the earlier "confirmed" authorised. The user never picked a provider, so the layer is written
**provider-agnostic** (any OpenAI-compatible `/chat/completions`) and stays **inert with no key**.

### The router refactor, and why it needed proving

Memory needs to know *which branch answered*, so `ask()` was split into `_route()` returning
`(intent, text, symbol)` and the four inline branches were lifted into `_decision_answer`,
`_market_answer`, `_risk_answer`, `_radar_answer`. `requests` was already a dependency, so no new package.

A refactor that touches ~120 lines of f-strings is exactly where a silent drift hides, so I spliced by
**line range** (`.scratch/splice_copilot.py`, dedent by 8) rather than retyping, then **differential-tested**
old vs new (`.scratch/diff_copilot_router.py`): **105 comparisons, 0 differences, EQUIVALENT** — 35 queries
× 3 contexts. Two harness bugs had to be fixed first, both worth remembering: `T._copilot` closes over the
`JarvisCopilot` imported at test-module import time, so both sides resolved to the NEW class and the
comparison was vacuous (`assert type(old_c) is not type(new_c)` now pins it); and `spec_from_file_location`
returns `None` unless the file ends in `.py`.

### A stub missing a field the code reads is a coverage hole

Writing a follow-up test for "analyze EURUSD" crashed on `d.symbol`. Not a product bug — the shared
`_decision()` stub in `tests/test_copilot_intents.py` omitted `symbol`, `regime`, `bull_case`, `bear_case`
and `quality_gate.passed`, all of which `ReasoningEngine.generate_explanation` reads. Consequence: the
copilot's **`analyze <symbol>` branch had never executed in any test** — `test_market_context_question`
passes no decisions, so it only reached the `contexts` branch. Enriched the stub from the real
`DecisionObject` schema and added `test_analyze_with_a_recorded_decision_renders_the_full_explanation`.

### Two safety properties, both mutation-tested

* **Memory carries a referent, never a fact.** It records `(intent, symbol)` — never an answer, price or
  P&L. The test that proves it mutates `latest_decisions` *between* the two turns and asserts the follow-up
  reflects the NEW value; a cached answer says WAIT instead of APPROVED and fails.
* **The model is additive only.** It is consulted *only* when the router returned the help text, so a
  grounded number can never be replaced by prose.

Mutations: letting the model override a grounded answer → 2 failures; putting the key in the request body →
1 failure; replaying a remembered answer → 5 failures. **My first M3 passed, which meant it had never
fired** — the injected branch read a `carried['_cached']` that does not exist. Rewrote it to actually cache
and it caught 5. A mutation that passes proves nothing.

### Round 12 baselines — full battery, all green

`pytest -q` **970 passed / 20 deselected** (was 924; +1 intents, +22 memory, +23 provider) ·
`verify_terminal_render.js` **55/55** + 5 CLEAR · `verify_dashboard_render.js` **183/183** ·
`verify_copilot_render.js` **23/23** · `verify_ui_layout.js` **238/238, 0 FAIL** ·
`verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **46/46** — its copilot probe still reports
`chars=479`, unchanged, which is the evidence the response body did not move · `audit_endpoints.py` 46/46
0 dead · `audit_wiring.py` 0 broken refs · `audit_css_tokens.py` 172/0.

Verified over HTTP against `.scratch/srv8611.py`: turn 1 "why aren't you entering XAUUSD?" → decision for
XAUUSD; turn 2 "what about EURUSD?" → **decision for EURUSD** (resolved, `session=true`); turn 3
"show current risk and exposure" → risk answer, **not hijacked**; `provider=false` with no key.

**Two curl traps hit again:** `curl -s -o /dev/null -w '%{http_code}'` exits **23**, so an `&&` chain built
on it silently skips every later step; and `--noproxy '*'` is required or a healthy local port answers
"upstream connect failed".

## Round 13 — the chart's trade markers had never been observed

Items 4 (chart markers) and 5 (Analyst/News beside the ticket) were the two delivered features with no
coverage at all — "subpanel" appears nowhere in `tests/`, and "marker" only as an unrelated dict name in
the copilot tests. Starting with item 4: `tools/verify_dashboard_render.js`'s chart stub had
**`setMarkers() {}`** — a no-op. So `drawTradeMarkers` was unobservable and every earlier assertion about
the chart passed without ever checking whether a marker was drawn. Exactly the round-8 trap again: "the
renderer was never called" and "the renderer works" are indistinguishable from outside.

Made `setMarkers` record, then **mutation-tested before trusting anything** — which is what exposed that my
first eight assertions were weaker than they looked:

* **"markers are sorted by time" could not fail.** Every event in this fixture falls *after* the loaded
  window, so all of them snap to the last bar and the order carries no information; deleting the `.sort()`
  left all 191 checks green. **Removed** the assertion and left a comment saying why — an assertion that
  cannot fail is worse than none.
* **The `closed_at` rule was untestable.** No XAUUSD row lacked `closed_at`, so deleting the
  `if (!t.closed_at) return;` guard changed nothing. Added fixture rows 70007 (a closed XAUUSD **SELL**, so
  the `wasBuy === false` branch is reachable — every other XAUUSD exit was a long) and 70008 (XAUUSD with
  `closed_at: null`).
* **A mutation can be blocked by an earlier guard.** Mutating `utcSeconds(t.closed_at)` to fall back to
  `timestamp` still passed, because the `closed_at` guard returned first. Only removing *both* produced the
  fifth marker — and failed 2 checks. Worth remembering: a mutation that passes may mean your mutation
  never executed, not that the guard is weak.

Adding the two rows shifted eight history-panel assertions; each new value was **re-derived, not pasted**
(net +43.95 = +83.95 − 40.00; 2W/3L over the six rows that carry a P&L = 33%, not 2/8 = 25%; XAUUSD rows
now 4; SELL rows now 3 at −98.20). 183 → **191 checks**.

**Still uncovered, reported not hidden:** the SELL *entry* direction (the fixture's only position is a BUY)
and the precise bar-snapping choice (every event is off-window, so all snap to the last bar). Both mutations
pass. Covering them needs a fixture change I judged riskier than the gap.

### A trap worth its own line

Python's `read_text()` / `write_text()` round-trip silently converts **CRLF → LF**. After restoring
`dashboard.js` from a mutation, `git status` reported it modified while `git diff` was **empty** —
`git ls-files --eol` showed `i/lf w/crlf`. `git checkout -- <file>` clears it. Do not conclude the file is
changed (or unchanged) from `git status` alone here.

## Round 14 — item 5, the Analyst/News context strip beside the ticket

Covered `setContext` / `renderContextAnalyst` / `renderContextNews` (`dashboard.js:2160-2253`), driven by
clicking the real `[data-ctx]` buttons. **No product code changed** — harness only.

**The stub was the bug, twice.**
1. `verify_dashboard_render.js` had no `[data-ctx]` buttons at all, so `bind()` attached nothing and the
   strip was unreachable. Added them (why/analyst/news + a fourth `bogus` value to exercise the
   normalisation), before the controller runs.
2. The two panels were created visible. `dashboard.html:392-393` ships them `hidden` with
   `data-state="loading"`, and **both renderers early-return while the panel is hidden**. With them
   visible, `renderDevilAdvocate()` (which also refreshes the analyst strip, `dashboard.js:1139`)
   populated the panel at tick 5, so deleting `renderContextAnalyst()` from `setContext()` still passed
   every check. Mirroring the markup made the tab's own render load-bearing.
   Use **`elementFor(id)`, not `registry.get(id)`** — the registry is a plain Map filled lazily, so `.get()`
   on an un-queried id returns `undefined` and the assignment silently applies to nothing.

**Timing is part of the fixture.** Captures are taken at ticks 17/19 — *before* the analyst view opens at
tick 40 and before tick 44's news reload — for the same reason: both of those re-render the strip, and a
capture taken afterwards cannot tell the tab's render from the panel's.

**Mutation-tested: 15/15 caught.** Two that mattered most: the strip reading the *wrong* symbol's decision
(EURUSD is authorised, XAUUSD is blocked → 5 failures) and the countdown falling back to the server's own
`diff_seconds` (stale after a tick → 1 failure). Only gap left: `if (!host || host.hidden) return;` — a
performance guard, genuinely unobservable from outside.

## Round 15 — the measurement spine

`tests/test_measurement_spine.py` (28 tests) now covers `tools/audit_trade_quality.py` (`metrics`,
`dynamic_regimes`, `replay`) and `tools/p0_1_direction_audit.forced_long_benchmark`. Only 2 of 36 Python
tools had any test, and these are the ones four P0 tools import. **Two errors in exactly these helpers
already changed a published verdict** (a biased always-long control: 1/20 → 3/20; and a 10× units error).
**Mutation-tested 22/22 caught**, including the two historical bugs re-injected deliberately.

**One real defect found:** `tercile()`'s small-sample fallback returned the literal `"MID"` for *both*
buckets. `vol_bucket` is a group key in `p0_3_exit_geometry_measurement.py`, so "MID" would surface beside
LOW_VOL/MID_VOL/HIGH_VOL; and "MID" is not in `trend_bucket`'s domain at all. Fixed to `labels[1]` (each
bucket's own middle label). Only product-of-a-tool change this round.

**A killed mutation run corrupts the tree.** The first battery attempt was SIGTERM'd at the foreground
timeout and left `sl_long = long_entry + risk` in `p0_1_direction_audit.py`; the second run then measured
every mutation against that corrupted baseline and reported a meaningless 21/22. **Run batteries in the
background, snapshot the files, restore in `finally` *and* `atexit`, and refuse to start unless the suite
is green and `git diff` holds only the intended change.** Also: a mutation script drives ~5s of pytest per
mutation, so a 22-mutation battery is ~2.5 min — over the 120 s foreground limit.

## Round 16 — remote auth: a hardcoded admin backdoor, removed

`jarvis/api/remote_auth.py` (379 lines, gates remote access to a live trading platform) had **zero tests**.
Writing them found four defects; the user approved removing the backdoor outright.

1. **CRITICAL — hardcoded admin passwords.** `verify_credentials` accepted `hm2026` / `hm2026admin` /
   `admin1234` for admin regardless of the configured password, and returned *before* the lockout
   bookkeeping, so they were immune to brute-force lockout too. The source is in a **public** repo.
   Removed. The configured password is untouched: `jarvis/.jarvis_admin_pass` (gitignored), or
   `JARVIS_ADMIN_PASS`.
2. **SERIOUS — `change_password` never checked the current password.** `if not cls.verify_credentials(...)`
   — a failed call returns `(None, "Invalid...")`, a non-empty tuple, therefore truthy, so the guard was
   always False. Any authenticated session could overwrite the password. Reachable at
   `POST /api/auth/change_password`. **A truthy-tuple guard is the same class of bug as a truthy string.**
3. **SERIOUS — the brute-force lockout was unreachable.** `check_rate_limit` defaults `client_ip` to `""`,
   `is_local_client("")` returned True, and both call sites pass one argument → *every* caller was
   "local" and got the relaxed 50 attempts instead of 5. Unknown now fails closed.
4. The admin password was logged in clear text at INFO. Now logs where it came from.

`tests/test_remote_auth.py` — 56 tests. **12/13 mutations caught**; the miss is genuinely unobservable
(`create_session_token` discarding from the revoked set — a new token has a fresh nonce, so it can never
already be in that set).

Two things found on the way, **not** changed:
* `server._is_local_request()` mints an admin session for loopback requests with no forwarded headers.
  Currently safe **only** because cloudflared sets `X-Forwarded-For` (and the Host check is a second
  guard). A tunnel configured without forwarding headers would hand admin to the internet.
* A `change_password` on **admin** is silently reverted by the next `_init_default_users()`, which re-syncs
  admin from the resolved password. There is no persistence layer, so no change survives a restart.

**Mutation-script traps (both cost a full re-run):** a multi-line anchor in a bash heredoc gets mangled —
write the script to a file instead. And `re.escape()` escapes the newline itself, so
`re.escape(s).replace(re.escape("\n"), r"\r?\n")` silently does nothing; escape each *line* and join.
This file also mixes CRLF and LF (408 of each), so try both endings.

### Round 16 baselines — full battery, all green

`pytest -q` **970 passed / 20 deselected** (unchanged — no product code moved) ·
`verify_dashboard_render.js` **191/191** (was 183) · `verify_terminal_render.js` **55/55** + 5 CLEAR ·
`verify_copilot_render.js` **23/23** · `verify_ui_layout.js` **238/238, 0 FAIL** ·
`verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **46/46** · `audit_endpoints.py` 46/46 0 dead ·
`audit_wiring.py` 0 broken refs · `audit_css_tokens.py` 172/0.

## Round 17 — `jarvis/market/sessions.py` (was 0 tests)

Chosen because it gates three things that reach real trades: `is_mkt_open` (from
`get_market_trading_status`), the Forex killzone hard filter, and the index prime window — plus
`is_in_killzone`, which `online_ml_predictor` turns into a learned feature.

**Defects found (all fixed):**
1. **Aware timestamps were never converted to UTC.** Only the *naive* branch was handled
   (`dt.tzinfo is None` → replace with UTC); an aware datetime had `.hour` read as if it were UTC
   although the field is literally `utc_hour`. `market_context:99` passes bar timestamps straight
   through when they carry a tzinfo, and MT5 timestamps run on the **broker clock** — same class as
   the broker-time defect. Fixed with a shared `_as_utc()` (astimezone, not replace).
2. **Killzones ignored the weekday.** Every other time gate here has a weekday guard; this one
   didn't → Saturday 08:00 UTC reported `LONDON_OPEN`. Weekends now: no killzone, no Asian range.
3. **`get_current_session` named a session at the weekend** (`LONDON_NY_OVERLAP` on a Saturday) —
   `is_prime_session` was already False, but the *string* goes into analyst narratives + copilot.
   Weekends now `OFF_HOURS`.
4. **Crypto detection was 3 hardcoded substrings** (BTC/ETH/SOL), so XRP/ADA/DOGE/AVAX/DOT/BNB —
   all served by `tradingview_provider._CRYPTO_SYMBOLS` — fell through to the 24/5 schedule and
   reported **closed on a Saturday**. Now `CRYPTO_TAGS`.
5. **Branch key asymmetry**: `reason` only on the weekend branch, `next_open_utc` missing on the
   crypto branch → KeyError for any consumer reading them from the other branch. Both now everywhere.
6. `hour > 21 or (hour == 21 and minute >= 0)` — `minute >= 0` is a tautology; it is just `hour >= 21`.
   Comments also wrong: Asian ran "to 09:00" (it stops at the 07:00 London open) and the schedule
   comment quoted EDT (an hour off for half the year).

**Not changed, flagged to the user:** `get_market_trading_status` treats **indices** (US30/NAS100/
US500) as 24/5 `FOREX_METALS_24_5` while `is_index_prime_session` in the same file says the US cash
market only runs 14:00-19:59 UTC. So the UI can show "🟢 MARKET OPEN (24/5)" for NAS100 at 02:00 UTC.
Which is right depends on cash vs CFD — and `decision_engine` can't be edited (read-only path), so
pointing both at one predicate is not something to do unilaterally.
Also `decision_engine:423` duplicates the old 3-tag crypto list; the registry's `is_crypto()` is the
designated source of truth but `resolve()` **logs ERROR for unregistered symbols**, so calling it
from this per-snapshot path would spam the log. Left alone deliberately.

`tests/test_market_sessions.py` — 192 tests. **22/24 mutations caught.**
*The 2 misses are provably unobservable, not test gaps*: `is_weekday` became redundant once the
weekend early-return was added, and `max(0, ...)` guards a difference that is always positive inside
the weekend branch. **A MISSED mutation is an equivalent-mutation candidate before it is a test gap.**

### Round 17 baselines — full battery, all green

`pytest -q` **1246 passed / 20 deselected** (1054 → 1246) · `verify_ui_layout.js` **238/238** ·
`verify_dashboard_nav.js` **31/31** · `audit_endpoints.py` 46/46, 0 dead ·
`verify_dashboard_render.js` **209/209** · `verify_terminal_render.js` **55/55** ·
`verify_copilot_render.js` **23/23** · `verify_ui_live.py` **46/46** · `audit_wiring.py` 0 broken refs.
Committed `26bdf29`.

## Round 18 — `jarvis/risk/trade_guard.py` (was 0 tests)

Picked by scanning: 121 modules, 19 with no test reference at all; `risk/` was the highest-value
gap because a defect there loses money silently. `TradeGuard.validate_pre_execution` is the last hard
gate before an order — `risk_engine` step 7, `rejection_reasons.extend(guard["reasons"])`.

**Defects found (all fixed):**
1. **Non-finite prices passed every check.** NaN compares False against everything, so `nan > cap`
   and `nan >= entry` are both False → a NaN spread/entry/SL/TP sailed through and was *approved*.
   An infinity passed one direction of each pair. Added `_is_finite()` over all four numbers.
2. **A bias that was neither BUY nor SELL skipped geometry entirely.** `if BUY ... elif SELL ...`
   with no `else` — so `"buy"` (lowercase) or `"NEUTRAL"` approved an inverted stop. Fail-closed now.
   **Checked before changing**: `tentative_bias` can be `"HOLD"` (decision_engine:126) and it does
   reach `DecisionObject.bias` (line 1206), but both call sites already restrict to BUY/SELL —
   `backtesting/engine.py:641` `entry_ok = ... and decision.bias in ("BUY","SELL")`, and
   entry_policy's "Directional Bias" capital-protection gate. So no live path changes.
3. **The Asian spread allowance read a tz-aware bar timestamp's wall-clock hour.** Same class as the
   round-17 `sessions.py` fix — window is documented 01:00-04:59 **UTC**, `market_context` passes
   bar timestamps through with tzinfo intact. Now `astimezone(timezone.utc)`.

**Pinned, deliberately not changed:**
* The spread cap comes from the **symbol registry**, not the caller. `getattr(spec,'max_spread_pips',
  max_spread_pips)` never falls back because SymbolSpec always has it — so risk_engine's 35.0 is
  inert. **Do not "fix" this**: BTC's registry cap is **3000 pips**, so honouring the caller's value
  would reject every crypto trade.
* `positions` is accepted and never read (correlation lives in risk_engine, not here).

`tests/test_trade_guard.py` — 78 tests. **21/21 mutations caught.** Two were missed on the first run
and both were the *same* real gap: the SELL "exactly at entry" tests only asserted `passed is False`,
which the *other* geometry check satisfied once the stop check was mutated away. **A test that
asserts only the aggregate verdict can be satisfied by a different failure — give every other input
a valid value so the test isolates one condition.**

### Round 18 baselines — full battery, all green

`pytest -q` **1324 passed / 20 deselected** (1246 → 1324) · `verify_ui_layout.js` **238/238** ·
`verify_dashboard_nav.js` **31/31** · `audit_endpoints.py` 46/46, 0 dead · `verify_ui_live.py`
**46/46** · `verify_dashboard_render.js` **209/209** · `verify_terminal_render.js` **55/55** ·
`verify_copilot_render.js` **23/23** · `audit_wiring.py` 0 broken refs. Committed `60ebcc8`.

## Round 19 — `jarvis/risk/hrp_allocator.py` (was 0 tests): **the module never implemented HRP**

`HierarchicalRiskParityAllocator` is imported by `tools/run_3month_backtest.py:254`, and
`reports/backtest_3month_report.md:244` presents the result under "## 5. Portfolio risk allocation
(HRP)". It has **never** done HRP. The first commit that added the file was already 48 lines with the
same two methods — no clustering, no quasi-diagonalisation, no recursive bisection — and
`get_correlation_distance`, the one HRP-specific primitive, has never been called by anything.
What it computes is **inverse-variance weighting**, so correlation is invisible: two assets that
move together each get a full weight and the portfolio can be far more concentrated than the weights
suggest. That is precisely what HRP exists to fix.

Fixed: docstrings now say what the code does (**class name kept** — `tools/run_3month_backtest.py`
imports it under that name, so renaming is a separate deliberate change). A test asserts
`get_correlation_distance` is *not* wired into `allocate_weights`, so implementing real HRP will
fail loudly instead of silently.

**Real numerical defect fixed on the way:** a zero-variance (flat) series took the **entire**
allocation. `variances[variances <= 0] = 1e-6` → inverse variance of a million → ~100% weight to the
asset the data says least about, everything else rounding to 0.0. In a backtest "flat" is exactly
what a symbol that never traded looks like. Degenerate series now get 0; if *all* are flat → equal
weight (not a divide-by-zero).

`tests/test_hrp_allocator.py` — 32 tests. **13/14 mutations caught.** The miss is provably
unreachable — again, not a test gap: the numerical-noise floor guards a *negative* variance, and the
diagonal of a covariance matrix is a sum of squares.

**Still untested (picked list, in priority order):** `market/news.py` (736), `intelligence/
master_confluence.py` (176), `intelligence/ai_dissector.py` (165), `historical/cli.py` (163),
`historical/regime_tagger.py` (105), `analysts/*` (7 files), `market/correlations.py` (88 — note its
`get_correlated_exposure_risk` and `compute_correlation_matrix` are **dead**, and `get_correlation`
returns **0.0 for any pair not in its 9-entry table**, so risk_engine:341's `corr > 0.70` gate never
fires for e.g. NAS100/US30; it also never sees inverse correlation, since -0.88 is not > 0.70).

### Round 19 baselines — full battery, all green

`pytest -q` **1356 passed / 20 deselected** (1324 → 1356) · `verify_ui_layout.js` **238/238** ·
`verify_dashboard_nav.js` **31/31** · `audit_endpoints.py` 46/46, 0 dead · `verify_ui_live.py`
**46/46** · `verify_dashboard_render.js` **209/209** · `verify_terminal_render.js` **55/55** ·
`verify_copilot_render.js` **23/23** · `audit_wiring.py` 0 broken refs. Committed `22d5bd9`.

## Round 20 — `jarvis/intelligence/master_confluence.py` (was 0 tests): one bad regime scored 0/100

`decision_engine:857` calls `score()`, then `master_confluence_valid = _master_score >= _min_confluence`
(18-24 by style/asset) and `if not master_confluence_valid:` blocks the order. So this is a **hard
gate**, not a descriptive score.

**The defect:** `reg`, `st`, `liq` were bound *inside* the first `try`, and every later block has its
own `except Exception: <component> = 0`. `reg` was resolved with `regime.primary_regime` — a **direct
attribute access** — so `regime=None` (or any object without it) raised in block one, leaving those
names unbound; each following block then died with a `NameError` that its own handler swallowed.

The result was not "one component scores 0" — it was **0/100 → WEAK → gate fails → order blocked**,
silently, with nothing logged. One unreadable input vetoed the trade.

Fixed: resolve `reg`, `st`, `liq`, `mo`, `vol`, `sess`, `df` **once, before** the try blocks, so a
failure costs one component, not all five. Added `_regime_name()` (tolerates None / str / enum
`.value`, falls back to "RANGE") and `_primary_frame()` (replaces the duplicated dead first
assignment — the original computed `df` at the top then *again* inside the VCP block, and the FVG
block read the second copy). Each handler now logs a warning naming the component.
`ai_score` is accepted and never read; a test pins that.

`tests/test_master_confluence.py` — 68 tests. **27/28 mutations caught.** The miss is provably
equivalent: `triple = 20` on the full FVG+OB+breaker stack can never differ, since those three
already sum to 20 and the component is capped at 20.

**Six of my first-run failures were my own arithmetic** — I forgot the `elif adx >= 15: trend += 2`
ladder and the `min(20, ...)` cap. Derive expected scores from the source, not from the docstring's
headline numbers.

### Round 20 baselines — full battery, all green

`pytest -q` **1424 passed / 20 deselected** (1356 → 1424) · `verify_ui_layout.js` **238/238** ·
`verify_dashboard_nav.js` **31/31** · `audit_endpoints.py` 46/46, 0 dead · `verify_ui_live.py`
**46/46** · `verify_dashboard_render.js` **209/209** · `verify_terminal_render.js` **55/55** ·
`verify_copilot_render.js` **23/23** · `audit_wiring.py` 0 broken refs. Committed `d73804c`.
