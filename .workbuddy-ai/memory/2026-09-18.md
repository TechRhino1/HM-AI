# 2026-09-18

Continues the 2026-09-17 session (rounds 23–24 are in that file).

## Round 25 — the six analyst workers (`jarvis/analysts/*_analyst.py`, all untested)

`tests/test_analyst_scoring.py` — **202 tests, 63/63 mutations caught**. Note the pre-existing
`tests/test_analysts.py` is a 2-test live integration smoke test; it was left alone.

These are the producers of `ai_score` (`decision_engine:718` = the plain mean of the six scores),
which is the `"AI Multi-Score Gate"` at `decision_engine:643`. So their **base levels** are
load-bearing, not cosmetic: bases are 50/50/50/**70**/65/**65**.

### Fixed

1. **`structure_analyst` — a missing H4 frame was reported as divergence.** `mtf_alignment` is built
   per trade style (`market_context.py:115`): SWING → D1/H4/H1/M15, DAY_TRADING → H4/H1/M15/M5,
   **SCALP → H1/M15/M5/M1 (no H4 at all)**. `mtf.get("H4") != bias and != "NEUTRAL"` is True when the
   key is absent, so every SCALP decision carried a fabricated "structural divergence (H4 is None)"
   risk factor. Now guarded with `h4 is not None`. **No score changes** — the branch never awarded or
   withheld points.
2. **`macro_analyst` — `news_calendar=None` and `[]` were the same thing.** `or []` + `if not ...`
   meant an explicit empty list still triggered the live `GLOBAL_NEWS_ENGINE` lookup. Now `None`
   means "ask the engine", `[]` means "no news". Production unchanged: the only caller is bare
   `MacroAnalyst()` at `parallel_runner.py:47`.

### Pinned, not changed

* **The +10 HTF confluence bonus is unreachable outside SWING** — it needs both `H4` and `D1`, and
  only SWING puts a `D1` in the dict.
* **A default/empty context scores 50/50/50/100/45/65, mean 60.0.** The volatility analyst reads
  `atr=0, spread=0` as "tight institutional spread" + "optimal volatility conditions" = **100/100**.
  A dead feed produces its best possible reading. Not changed: it would move every score by up to 15
  and invalidate the signal-quality baselines in `AUDIT-2026-09.md`.
* Volatility's excessive-spread branch **assigns** `10.0` (does not subtract), so the state
  adjustment lands on top: excessive + NORMAL = 25, excessive + EXTREME = 0.
* The risk analyst's BEARISH stop guards on `supply_zone[1] > 0`, not `> current_price` — a stale
  zone below price inflates the stop instead of falling back to ATR. (The BULLISH side uses
  `> 0` too; neither is `> price`.)
* A macro shock that does **not** apply to the symbol (AUDNZD is in neither map) still suppresses
  the structure-bias fallback, leaving NEUTRAL.
* Momentum and structure both reward a strong downtrend exactly as much as a strong uptrend
  (+20 / +35) — the score carries magnitude, the bias carries direction.
* Neutral bias in either premium/discount zone scores **+15**, not 0, with evidence calling the setup
  "optimal" for a direction the report does not hold.
* `_parse_metric` handles %/K/M/B but **not thousands separators** — `"1,234"` is unparseable.

### Round 25 baselines

`pytest -q` **1896 passed / 20 deselected** (1694 → 1896) · `audit_wiring.py` 0 broken refs ·
`verify_ui_live.py` 46/46. Committed `235052a`; remote = local, tree clean.

Remaining untested: 20 modules — largest `api/intelligence_api.py` (868), `market/news.py` (736),
`stocks/stock_service.py` (689).
