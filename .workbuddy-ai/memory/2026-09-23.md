# 2026-09-23

## Three-track audit: phases 1-4 committed (35f13bf)

Report: `docs/AUDIT-3-TRACKS-2026-09-23.md`. User approved "phases 1-4 now, defer
entry-model + FVG", and "keep the loopback auth bypass for now".

* **Phase 1** deleted 16 root scripts (10 scratch + 6 `test_*`). `get_remote_mobile_access.py`
  **printed a hardcoded admin password** — it is out of the tree but still in git history,
  so that credential must be rotated. Also deleted 7 unreferenced calibration snapshots
  (`config/winrate_profiles.{BAK,MARGIN0,MARGIN17,PRE_FIX,PRE_SLIPPAGE,UNGUARDED,WIDEGRID}.json`,
  ~690 KB); only `config/winrate_profiles.json` is ever loaded.
* **Phase 2** `/health` + `/ready`, security headers, token revocation with pruning,
  ADMIN-only 12-char password floor, WAL + busy_timeout on the two risk DBs.
* **Phase 3** `jarvis/common/timeout_guard.py` moved out of `jarvis/application`.
* **Phase 4** scan `ThreadPoolExecutor` hoisted to a module singleton.
* Deferred by design: entry-model changes (Phase 5), FVG swap (Phase 7), loopback-bypass
  tightening, wrapper consolidation, per-IP throttle.

## Traps confirmed this window

* **A bulk `git rm` triggered the vanish incident.** Deleting 16 root files + 7 configs
  made the entire `config/` directory disappear from disk mid-command (`ls: cannot access
  'config/'`). Recovered with `git checkout -- config/` — the index restore correctly kept
  the staged variant deletions. **The trigger here was a bulk `git rm`, not `git stash`.**
* **Phase 1 deletions were silently lost once** before this. The prior window reported them
  done; they were all still `PRESENT` and tracked. Always re-verify a deletion with
  `[ -f "$f" ]` — a "done" report from a sub-agent is not evidence.
* **`git rm -rf --cached <dir>` + `rm -rf <dir>` on an archive copy is a trap.** Phase 1
  had *copied* the junk configs into `config/archive/` instead of moving them, so
  `git add -A` staged 23,446 lines of byte-identical duplicates. Check `git diff --cached
  --numstat` before committing — a cleanup commit that adds 23k lines is a cleanup that
  failed.
* **A 404 on a new endpoint usually means a stale server, not a broken route.** `/health`
  returned a 404 HTML error page because the dashboard on 8501 was started *before* the
  edit and Python has no hot-reload. Don't debug the route; restart the process. To verify
  **without killing the user's running dashboard**, boot a second instance in-process on a
  spare port, probe it, and `srv.shutdown()` — see `.scratch/verify_health.py`.
* **`pytest.ini` has `testpaths = tests`**, so root-level `test_*.py` scripts were never
  collected — deleting them cannot move the suite count. Check this before worrying that
  removing test-named files breaks the baseline.
* **Never commit with `--no-verify`** (habit from other repos). This repo has no active
  hooks (only `.sample` files), so nothing was lost, but the flag was wrong.

## ⚠ The audit's headline root cause was WRONG — both entry models measured out

Two research agents ran the backtests that gate Phases 5 and 7. Both came back negative, and the
stop-floor one **overturned my own report's headline**.

* **"Median stop 0.21×ATR" does not reproduce.** Measured on 46,939 candidates: median candidate
  stop is **2.58×ATR (H1)**; only 0.2–3% sit below 1.11×ATR. Origin of the bad figure: a
  **daily-vs-H1 ATR unit error** — `symbol_registry.py:22` documents `typical_atr_pct` as *daily*
  ATR %, but `dynamic_levels.py:142` divides it into the *H1* `vol.atr`, so `atr_ratio` pins at its
  0.33 clamp on **69.8% of bars (FX 90–99.6%)**. The "volatility-adaptive buffer" is a constant.
  Consequence: **raising the stop floor is a measured no-op** (ΔE[R] = +0.00092; arms
  0.10/0.30/0.50 come out bit-identical). Knee ≈0.70×ATR and we are already past it.
* **FVG standalone: DO NOT PROCEED.** 0/20 pass DSR; Sharpe **negative on all 20**; win rates
  12–34.7% vs 42–59% needed at 1.5R. Loses less than always-long, which is not an edge.
* **"327 independent bets" is wrong** — internal proof: one symbol alone measures 3,971. Real
  ratio **4.2%, not 0.3%**. `deflated_sharpe_report.py:239` passes raw span entries to
  `effective_sample_size`, which wants `List[Dict]` trade records. Direction: effective n is
  *larger*, so the DSR bar is *less* severe than I reported — conclusion unchanged, severity was wrong.

**Rule: a ratio whose numerator and denominator come from different timeframes is not a
measurement.** Check the unit before quoting a multiple of ATR. And **a stop floor that changes
nothing across a 10× range is not the binding constraint** — prove a knob binds before tuning it.

New production bugs found (reported, not fixed): dead ICT gates (`has_mss` computed :97 never
read; `h1_aligned` :212 only stored; `in_kill_zone` :225 only relabels — both branches set the same
`entry_price`); `dynamic_levels.py:514-539` returns the institutional result *instead of*
`base_result` when `mtf_data` is present, so the :273/:404 floor may never govern a live stop;
`institutional_entry_engine.py:131,341,348` floors `risk_dist` without moving `sl_price`.

## Trade outcomes now persisted (0d0f0ef) + two more inert levers

`executed_trades` had no `exit_price` / `realized_pnl` — `realized_pnl` was only ever synthesised
in-memory at request time from the MT5 deal (`server.py:669`), so nothing was learned from a closed
trade. Now: `SCHEMA_VERSION` 4→5, migration adds both columns and backfills the old `DEFAULT 0.0`
sentinel to NULL **only where `closed_at IS NULL`**; `record_trade_exit()` writes from the closing
deal (`.price`/`.profit` confirmed on `TradeDeal`); `orchestrator._on_trade_closed` calls it.
**Unknown is NULL, never 0.0** — 0.0 is a real P&L *and* what `dict.get()` returns for a missing
key; that ambiguity produced the "42/93 rows with pnl=0" finding. Four tests pin it.

Suite now **3089 collected, 0 failures, 0 errors** (junit-verified, not the summary line).

**ATR unit fix also measured inert (0d9375a):** 3 outcome flips in 46,939; ΔE[R] ≤ 6e-5. Because
`atr_ratio` is **dead code** — it feeds only `max(dynamic_buffer, anti_wick_buffer)` and the dynamic
term needs `atr_ratio + spread_ratio > 4.6` to beat the fixed 0.35×ATR anti-wick buffer. Median
`effective_buffer` = **0.3500×ATR in every arm**. The "volatility-adaptive buffer" was never
delivered. Also: BUY `struct_sl_dist` omits `spread_dist` where SELL adds it (:227/:229 vs
:363/:365) → SELL stops systematically one spread wider for identical structure.

**Three stop levers, all inert** (floor, ATR unit, FVG). Stop geometry is not what loses money.
Stop tuning that class of knob; the entry signal has no edge and no geometry parameter fixes that.

## The unknown-vs-zero defect class, three layers

`0.0` is a real P&L **and** what `dict.get()` returns for a missing key. That ambiguity was
repaired in three places, last one highest-risk:

1. `executed_trades` — no `exit_price`/`realized_pnl` at all; P&L existed only in a request-time
   dict from live MT5 deals. Added both columns + `record_trade_exit()` (NULL, never 0.0).
2. `trade_records` / `jarvis_trade_memory.db` — `record_trade` wrote `exit`/`pnl`/`is_win` as
   `0.0`/loss **at open**. Now NULL + `update_closed_trade()` (UPDATE, not INSERT OR REPLACE).
   No backfill of the 42 historical rows: with no `closed_at` marker, `exit_price=0 AND pnl=0`
   cannot be told apart from a real break-even whose exit price was unavailable. Guessing is worse.
3. **`_on_trade_closed` (orchestrator) — the dangerous one.** `pnl = float(data.get("pnl", 0.0))`
   turned a missing P&L into a real break-even, and `is_win = 1 if pnl > 0 else 0` filed it as a
   **loss**, which then fed `circuit_breaker.record_trade_result(is_win == 1)` — so **a data gap
   could help trip the circuit breaker on losses nobody observed.** Also poisoned the bandit reward
   and the ML label. Now Optional; each consumer withholds an unknown sample.

**Rule: before trusting a `.get(key, 0.0)` fallback, ask whether 0 is a real value AND whether the
consumer treats falsy as "bad".** A missing measurement must be `None`, and every consumer must be
checked — the danger is not the fallback, it's `is_win == 1` downstream.

## Late findings — two more premises overturned

* **The institutional/ICT engine is unreachable live.** `DecisionEngine.evaluate()` accepts
  `mtf_data` and both live drivers pass a populated dict, but **never forwards it** to
  `_compute_bias_and_levels` (decision_engine.py:712-716). So `calculate_levels` gets
  `mtf_data=None` → `base_result` → the `:514` early-return never fires → **the baseline floor DOES
  govern live stops** (premise valid). But it also means the FVG/ICT engine only runs via
  `calculate_manual_trade_levels`. My earlier line "FVG is already shipped and the system still
  loses" was the wrong reason — **it was shipped and never exercised.** The negative backtest
  verdict still stands on its own.
* **Live spread is a hardcoded constant.** Every `build_context` passes
  `current_spread_pips=spec.typical_spread_pips` (orchestrator.py:679, server.py:257). Registry
  understates FX majors 2.0–2.9× (EURUSD 0.7 vs 1.9 measured; USDJPY 0.8 vs 2.3); WTI over 4.3×.
  So: cost/EV understated ~2.7×, **the spread rejection filter can never fire** (typical<max always),
  the floor's spread term is ~2.7× too tight, and `spread_ratio` is pinned at 1.0 → `gamma_spread`
  inert.

## Stop-distance floor fixed (90eaa28)

`institutional_entry_engine` floored `risk_dist` at `pip_size*5` without moving `sl_price`, so sized
risk and reported R:R were decoupled from the real stop. **Six sites, not the four I specified — a
third BUY/SELL pair (DAY_TRADING 248-256/257-265) I had missed.** Now: floor the distance, derive
`sl_price`, `risk_dist = abs(entry-sl_price)`. Tests proven non-vacuous (12/19 fail pre-fix).

**Rule: after fixing a duplicated bug, grep for the pattern, not the line numbers I listed.**
A defect copy-pasted across protocol branches will have more copies than the first survey finds.

Suite **3111 passed / 0 failures / 0 errors** (junit `tests=3132`).

## Last two levers: both MATERIAL, both withheld (needs the user's call)

The "measure, ship only if immaterial" gate caught two more. Neither shipped.

* **BUY/SELL spread symmetry — SHIPPED (a6d2733, user-approved).** Widens BUY stops by one spread
  at **7 sites** (:227,:229,:232,:235,:239,:243,:270) to match SELL. 43 tests, 30 fail pre-fix.
  Suite 3154 passed / 0 failures. Recovers ~5% of the loss — **not profitability**.
* **BUY/SELL spread symmetry** — SELL charges `spread_dist` in its structural stop, BUY does not, so
  SELL stops are ~1 spread wider for identical structure. Fix widens BUY stops (pooled median
  0.175×ATR). **ΔE[R] +0.0055..+0.0086, t=+3.9..+5.1, 406-733 trades change exit reason** —
  favourable but NOT inert (6-9× the inert benchmark). ~5% of the loss; does not make it profitable.
  **7 sites, not 3**: :363,:365 structural, :368 SCALP, :373/:377/:381 DAY caps, :399 SWING cap —
  all-or-nothing, a 3-site fix leaves DAY/SWING asymmetric. 43 tests, 30 fail pre-fix. Patch in
  `.scratch/spread_symmetry_fix.patch`.
* **Spread calibration** — correcting the registry is material and **adverse**: EXECUTE 2440→2839
  (+16%), total R −115→−240. Not just cost: `typical` feeds Forex-Prime-Session / AI-Multi-Score /
  Calibrated-Win-Prob gates, so it changes *selection*. Withheld.

**Root cause found:** `market_context.py:79-80` sets `bid=close`, `ask=close+current_spread_pips*pip_size`,
and every live caller passes `current_spread_pips=spec.typical_spread_pips` (orchestrator.py:679,
server.py:257, dynamic_levels.py:590). So at dynamic_levels.py:139 `ask>0 and bid>0` is ALWAYS true
and `spread_dist = typical*pip_size` — **the measured spread is never consulted and the fallback is
unreachable. The spread rejection filter and blowout guard are dead, since `spread_ratio ≡ 1`.**

## Baselines / traps

* **Never measure a baseline while another process is still writing test files.** My "3062" was
  taken mid-write and undercounted by ~13; the real figure was 3075+. Verify counts from the
  **junit XML**, not the stdout summary (the harness truncates it).
* **`git add -A` while a teammate is mid-task sweeps up their in-flight work.** It happened here —
  a report-only commit swallowed `database.py`, `server.py`, `orchestrator.py` and a new test file.
  Scope the add (`git add <paths>`), or check `git diff --cached --stat` before committing.
* **A safe-delete hook can block a pytest run** when a turn accumulates bulk deletions (agents
  writing/removing backtest temp files hit a 4667/50 threshold). Don't retry; read the junit XML.
* **A stale `--basetemp` produces 168 bogus `assert not self._finalizers` setup errors** from inside
  `_pytest/fixtures.py:1221` — pytest's own bookkeeping, with **`failures: 0`**. Do not read that as
  a regression. Always pass a **fresh** `--basetemp` path. Signature to recognise: errors (not
  failures), on `setup`, traceback entirely in `_pytest/fixtures.py`.
* **Suite now 3154 passed / 0 failures / 0 errors** (junit `tests=3175`).
* **`dict.get(key, 0)` does NOT protect against a present-but-None value.** After making outcome
  columns nullable, `t.get("exit_price", 0) > 0` raised `TypeError: '>' not supported between
  NoneType and int` (orchestrator.py:479). **Two tests failed only in a FULL run — a scoped pass hid
  it** (different rows in the shared trade_memory). Grep for `.get(col, 0)` after nullability
  changes, and **always verify with a full run, never only the scoped subset.**
* **An order-dependent failure is the signature of shared-state leakage between tests** — scoped
  green + full red means another test left a row behind, not that your change is fine.

## Two defects my own Phase 2 work shipped (fixed in 523f47e)

Caught only by the regression tests written for it — neither was visible by reading.

* **`/health` answered "ok" about subsystems it never probed.** `_send_health` stores a probe
  exception as `{"error": ...}`, then reads the verdict with `.get("held")` / `.get("wedged")`.
  Those keys are absent from an error dict, so both read as healthy and the endpoint returned
  200/"ok" while having probed nothing. General rule: **a probe that returns an error dict must
  not be read with `.get()` for a verdict key — absence of a key is not a healthy reading.**
* **A revoked token outlived its own expiry** — membership was tested before the sweep ran.
  General rule: **sweep before you test membership**, or the stale entry wins one last time.

## New tests

Suite now **3062 passed / 1 xfailed / 20 deselected / 0 failed** (was 3004). +58 tests.

* `tests/test_entry_geometry_invariants.py` (18 passed, 1 xfailed) — trade-entry geometry:
  entry is the raw ask/bid (documents that entries are NOT snapped to S/R), stop side and
  sign, `risk_dist` == |entry − sl| (regression guard for the ~7x risk blow-up), TP side,
  RR self-consistency, and the refuse-don't-fabricate path for unobserved/negative prices.
  Two tests are **characterisation** tests pinning the known defect: the stop floor is
  `max(3 × spread, 0.10 × ATR)`, and an `xfail` asserts the Phase 5 target of 1.11 × ATR.
