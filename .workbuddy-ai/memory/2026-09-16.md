# 2026-09-16

## Session 18 — push pending work, 365d audit, MEMORY.md split

* **MEMORY.md was 15.7 KB and silently truncating at the tail on injection.** Fixed *structurally*
  for the first time instead of another compression pass: moved the deep audit detail into
  **`AUDIT-2026-09.md`** (7.3 KB, read on demand) and left MEMORY.md as pure operating facts
  (10.8 KB). MEMORY.md now ends with a pointer. Lesson: when a note file keeps busting its budget,
  **split by topic and link**, don't keep rewriting it.
* Pushed `25c18ee` (365d audit) + `fbaa289` (memory split). Remote verified `fbaa289` via
  `git ls-remote` — matches local.

## 365d re-validation — final corrected numbers (SWING/H1, tp1.5, costs)

Full table lives in `reports/trade_quality_audit_365d.json`. 183d → 365d:

| | 183d | 365d |
|---|---|---|
| PF ≥ 1.0 | 4 / 20 | 7 / 20 |
| PF ≥ 1.3 | **0 / 20** | **0 / 20** |
| mean PF | 0.8781 | 0.9621 |

* 17/20 symbols improved (mean PF +0.084). XAUUSD is now best (1.159, +0.0889R, DD 177R),
  overtaking WTI (1.202 → 1.039). AUDUSD worst on both (0.715, −0.1951R).
* **The acceptance criterion is still met by nothing.** PF ≥ 1.3 is 0/20 on *both* windows, and
  13/20 still have negative expectancy at 365d.
* Caveat worth repeating: cost basis here is slippage 0.5 pip + commission 0, **not** the full
  honest-cost model, so 7/20 is optimistic.
* Comparison trap hit again: reports key results `SWING(H1)` / `DAY_TRADING(M15)` / `SCALP(M5)`.
  A naive loop lets SCALP overwrite SWING and prints nonsense ("20/20 PF≥1.0, mean 1.37").
  **Always filter by style when comparing reports.** Added to MEMORY.md.

## Platform state at 05:0x

* `/api/diagnostics` → OPERATIONAL, MT5 CONNECTED, DATA_FEED STREAMING. Account 101059540
  XMGlobal-MT5 5, **name "Demo Account"**, balance **764.42** (was 779 at session start, then
  771.58) — the two live tickets lost ~14.6 and are closed.
* **The server started 01:47; both fix commits landed later** (955d40c 03:39, 189c1e2 03:58).
  No hot-reload ⇒ **the running platform still has the 7× risk defect and the non-volatility
  sizing.** Needs a restart.
* **Mode is currently PAPER** per `/api/telemetry_state`, although the session *did* route real
  broker tickets at 01:47:25 (#933755581 SELL 0.69 EURUSD#, #933755583 SELL 0.25 AUDUSD#), so
  live execution works when enabled. `/api/action/set_mode` is the switch. Treat "what mode am
  I in" as something to verify, not assume — the log said LIVE and telemetry said PAPER.
* One stale position: `#3927160 [PAPER] BUY 0.01 GOLD.i# @ 2400.0` with **impossible geometry** —
  SL 4930.3 and TP 4395.59 are *both* above entry. For a BUY the stop is effectively acting as a
  second target. This ticket has been ratching SL upward since 01:55 (4293.63 → 4295.31 → 4930.3).
* Two PIDs (25808, 12328) both LISTENING on 8501 — Windows SO_REUSEADDR. Polled 8× and got
  identical PAPER responses, so it is not two disagreeing servers.

## Clock note

The harness `current_time` said "2026-09-15 23:19 GMT+5:30" while `date` said
**Wed Sep 16 05:04 IST 2026** — the injected time was stale by ~5.7 h. Git commit dates
(09-16 04:xx) were right; the harness was wrong. **Don't order events by the injected timestamp.**

---

## Session 13 — the six vanished files, and the UI redesign

### The blocker: six files deleted, then uncreatable

`git status` showed six unstaged deletions — `HM_dashboard.bat`, `HM_start.py`,
`jarvis/data/schemas.py`, `jarvis/intelligence/decision_engine.py`,
`jarvis/intelligence/honest_base_rates.py`, `tests/test_honest_base_rates.py`. The app could not
boot (`ModuleNotFoundError: jarvis.data.schemas`, imported by all of `jarvis/analysts/*`).

Every write path failed: `git checkout --` (rc 255, *unable to create file … Permission denied*),
shell redirect, `open(p,'wb')` (`Errno 13`), `os.replace` (`WinError 5`), the Write tool (`EPERM`).

**The diagnosis that mattered:** `os.stat` returned `FILE_NOT_FOUND` *while* Win32
`CreateFile(CREATE_ALWAYS)` returned `GetLastError=5` (ACCESS_DENIED). A deny ACE would let `stat`
succeed — so this was **delete-pending**: the name reserved by an open handle with delete-on-close.
`psutil` found no holder, but **181 of 301 processes raised AccessDenied**, so the holder (SYSTEM:
Defender/OneDrive) was invisible from user mode. Not worth chasing further.

**The recovery:** `os.link(alias, target)` creates the reserved directory entry where `CreateFile`
cannot, and because both names are one file object, writing through the writable alias fills the
blocked path. My first attempt failed with `LINK-FAILED … cannot find the file specified` — the bug
was mine: I deleted the alias before linking it, and **`os.link` needs the SOURCE to exist first**.
All six restored and SHA-1 verified against `git show :<path>`. `git diff` is empty (only
`core.autocrlf` normalisation), so the content is genuinely intact.

### The UI redesign (the actual request)

Liquid glass + mobile app shell, committed **`7248f59`**, pushed and remote-verified.

* **Glass** lives in `hm_ui.css` (shared by all six pages) as tokens plus a `.hm-glass` utility;
  `theme_terminal.css` (dashboard only) adds an ambient radial background, because a blur over a flat
  colour is invisible. Two traps cost real time: the sheen/tint must be `background-image` **layers**
  (a positioned `::before` paints *above* non-positioned text), and the `@supports not
  (backdrop-filter…)` fallback must come **after** the rules it overrides or it loses on source order
  and ships a translucent, unblurred panel.
* **The responsive defect was measurable, not aesthetic.** `.tt-app` declared `grid-template-rows`
  but no column, so its implicit `auto` column was sized by item min-content — a 404px tab strip made
  `documentElement.scrollWidth` **421px on a 390px viewport, on every page and every dashboard view**.
  7 of 33 captures overflowed by +31px. Fixed with an explicit `minmax(0, 1fr)` column plus
  `min-width:0`/`overflow-x:auto` on the strip. **After: 0 of 33**, at 360/390/768/1440. My first
  attempt made it *worse* (145px) because `flex-basis:100%` on the strip resolved against content in a
  content-sized column.
* **A bottom tab bar is not reachable here.** A `position:fixed` descendant of `.tt-rail` cannot blur
  page content: the rail's `z-index` creates a stacking context, and `backdrop-filter` on an ancestor
  also creates a containing block for fixed children. Hence a top app bar with a swipeable icon tab
  strip, not a bottom nav. Icons are CSS `::before` glyphs so they inherit the selected colour.
* **The two `Failed to fetch` panels were not a code defect.** `apiRequest` already has a `.catch`
  that normalises every failure to a resolved `{ok:false, status:0, error}` — the promise never
  rejects, so the missing `.catch` I set out to add would have been dead code. The panels were
  *correctly rendering* the error. Reading the user's four screenshots settled it: Performance and
  Risk showed live data (BALANCE 771.58, GOLD# 0.01) in the same frame, so the **server died
  mid-session** — telemetry had loaded, then these two requests got connection-refused.
  The real defect was that `error` was the browser's literal **"Failed to fetch"** shown to a trader.
  Fixed centrally in `dashboard.js:apiRequest` and `console.js:getJSON/postJSON` (the latter corrects
  ten catch sites without editing any). Also worth recording: **the console "Failed to fetch" line is
  Chrome's own network log, not an unhandled rejection** — do not "fix" it by adding catches.
* Also cleared a `404 /favicon.ico` on every page with an inline SVG icon in all six templates.

### Verification

`pytest` **765 passed / 20 deselected** · `verify_dashboard_render.js` **88/88** ·
`verify_dashboard_nav.js` **31/31** · `audit_endpoints.py` **44/44, exit 0** (the three India routes
that used to hang now return 200) · overflow **7 → 0**. Screenshots at real viewports via
`puppeteer-core` + installed Chrome; `.scratch/shots.js <tag>` is now a reusable before/after harness
that reports horizontal overflow and console errors.


---

## Session 14 — dashboard layout redesign: hierarchy, space management, and the defect underneath

**Request:** reorganise and rearrange existing dashboard content into a clear logical hierarchy,
optimise screen space, improve grouping/spacing/alignment, keep it responsive.

### What was restructured (template)

Five monolithic panels each nested a second `.tt-panel__head` *inside* their `.tt-panel__body`, so a
sub-section was drawn with the same weight and band as a peer panel — the hierarchy read flat and the
eye had no entry point. Each became a real panel with its own head, body and grid slot:
Analytics Performance → `perf`+`reliab`+`hist`; News Next release → `next`+`detail`; Markets Global
equities → `equities`+`sector`; Analyst Quality gate → `gate`+`obj`; Backtest Progress →
`prog`+`results`+`jobs`. **18 `tt-slot--*` classes** now exist and `grid-template-areas` places them
by name, so placement is stated once instead of being inferred from DOM order and patched by
breakpoint overrides (which is how two `max-width` blocks came to disagree about a view's column
count). Every element id preserved — `verify_dashboard_render.js` 88/88 after each edit.

Also removed four inline `max-height: 32–46%` attributes. A percentage cap with no matching
`flex-grow` is the worst of both worlds: a dead band on a tall screen, a collapsed body on a short one.

### THE REAL DEFECT — `min-height` where `height` was meant

`.tt-app` declared `min-height: 100vh`. With an **indefinite** grid container height the `1fr` main
row resolves against **content**, so a view grew to its own height: **Trade measured 1263px tall on a
900px viewport.** The document scrolled, and every panel that was supposed to fill the screen was cut
off at the fold instead. Fix: `height: 100vh; height: 100dvh` + `overflow: hidden` on `.tt-main` +
`overflow-y: auto` on the active view (Trade opts out; its panes scroll internally). Result: view 734
≤ main 758, `docScrolls=false` at all four viewports. **This is the same family as the implicit `auto`
grid column** — a definite size that was never stated, silently resolved from content.

### Two more real defects the new checks found

* **A panel head can demand more width than the panel has, and `overflow: hidden` eats the surplus
  silently.** The Economic calendar head holds four controls plus a chip: **626px of min-content in a
  372px phone panel**, so the last select and the Refresh button were simply not drawn. Fixed with
  `height: auto; min-height: var(--tt-head-h); flex-wrap: wrap` on `.tt-panel__head` — `flex-wrap` is
  "wrap only if it does not fit", so a head with room is pixel-identical and no breakpoint is guessed.
* **Analytics gave the 8-column regime table a 1.35fr column while a half-empty panel sat beside it**,
  so the BASIS column scrolled sideways through a mid-word cut. The regime table now takes a
  full-width row beneath a 2x2 block of the short account panels: overview metrics above, dense detail
  below.

### Tooling

`tools/verify_ui_layout.js` grew three checks per view (154 → **238**): **document must not scroll**
(app shell), **view must fit inside main**, and **no silently clipped content**. The clip check
exempts `text-overflow: ellipsis` (designed truncation), `.tt-sr-only`, form controls, and
`#chart`/`#chart-tv` (lightweight-charts' own generated canvas) — a check that fires on those stops
being read. It immediately found the calendar-head defect. Also fixed: both puppeteer tools hard-required
`puppeteer-core` from a repo that has no `package.json`, so they only ran when `PUPPETEER_ROOT`/`NODE_PATH`
happened to be exported; both now resolve the managed node workspace themselves. `.scratch/probe_panels.js`
is a reusable panel-clipping probe.

### Verification

`pytest` **765 passed / 20 deselected** · `verify_dashboard_render.js` **88/88** ·
`verify_dashboard_nav.js` **31/31** · `verify_ui_live.py` **44/44** · `audit_endpoints.py` **44/44,
0 dead** · `audit_wiring.py` 0 broken refs · `verify_ui_layout.js` **238/238**.
Screenshots in `.scratch/shots/final3/`.

### Carry-over resolved

`data/models/meta_labeler.joblib` (121 KB, generated, self-trained when absent) → now covered by a
`data/models/*.joblib` rule in `.gitignore`; the numbers it encodes are already committed in
`meta_labeler_report.json`. `data/real_universe_manifest_365d.json` (4 KB) is tracked, matching its
already-tracked sibling `real_universe_manifest.json`.

---

## Session 15 — the broker-clock defect: MT5 times are not UTC

Picked up the last open backlog line ("Broker server time labelled as UTC (XM is GMT+2/+3) skews
session logic"). It was worse than the line suggested: it **disabled the stagnation exits.**

MT5 reports `position.time`, `deal.time` and `symbol_info_tick().time` as seconds on the **broker
server's** clock. `mt5_client.py:240` (`time.gmtime(p.time)`) and `database.py:201`
(`datetime.fromtimestamp(t, timezone.utc)`) each labelled those values UTC, putting them **2-3 hours
in the future**. `position_monitor._get_position_duration_sec` computes `now_utc - open_dt`, so the
holding time came out **2-3 hours short**, and `max(0.0, ...)` flattened it to a hard **zero for the
first three hours of every position's life**. The horizon-adaptive block is gated on
`if open_dur_sec > 0:`, so the whole time-decay section was skipped in exactly the window it exists
to police:

* SCALP "45 min without progress" could not fire until **3h45m**; DAY 6h fired at **9h**; SWING 36h
  at **39h**.
* `_determine_position_style` falls back to duration, so a **25-hour-old position read
  `DAY_TRADING` instead of `SWING`** — and style picks the exit rules and timeframe.

**Fix.** New `jarvis/data/broker_time.py`: derives the offset from the broker's own **freshest tick**
rather than hardcoding it (moves with the broker's DST, differs per broker — +5:30 India — and
survives a server change). Snapped to 1800s with a 300s tolerance plus a ±12/14h range check, so a
**weekend-stale tick is rejected** instead of producing a nonsense offset. Cached 600s (each
derivation is an MT5 round-trip per symbol). On failure returns 0 — the old behaviour — and **logs a
warning once**. `get_open_positions` and `sync_mt5_history` now stamp true UTC; the `max(0.0, …)`
clamp now warns instead of silently zeroing.

**The lesson worth keeping:** a silent clamp is a bug hider. `max(0.0, negative)` did not merely
produce a wrong number, it converted a loud sign error into a plausible zero that also happened to
satisfy the caller's `> 0` guard.

`tests/test_broker_time.py` — 13 tests; **3 fail on the pre-fix code**, including
`'DAY_TRADING' != 'SWING'`. Demonstrated by temporarily restoring the buggy line and re-running.

Verified: `pytest` **778 passed / 20 deselected** (was 765, +13); `audit_wiring.py` 133/133 modules
import, 0 broken refs, 0 import failures.
