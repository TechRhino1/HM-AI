# 2026-09-16

## Session 18 — push pending work, 365d audit, MEMORY.md split

* **MEMORY.md was 15.7 KB and silently truncating at the tail on injection.** Fixed *structurally*
  for the first time instead of another compression pass: moved the deep audit detail into
  **`AUDIT-2026-09.md`** (7.3 KB, read on demand) and left MEMORY.md as pure operating facts
  (10.8 KB). MEMORY.md now ends with a pointer. Lesson: when a note file keeps busting its budget,
  **split by topic and link**, don't keep rewriting it.
* Pushed `25c18ee` (365d audit) + `fbaa289` (memory split). Remote verified `fbaa289` via
  `git ls-remote` — matches local.

## 365d re-validation — final corrected numbers (SWING/H1, tp1.5, costs)

Full table lives in `reports/trade_quality_audit_365d.json`. 183d → 365d:

| | 183d | 365d |
|---|---|---|
| PF ≥ 1.0 | 4 / 20 | 7 / 20 |
| PF ≥ 1.3 | **0 / 20** | **0 / 20** |
| mean PF | 0.8781 | 0.9621 |

* 17/20 symbols improved (mean PF +0.084). XAUUSD is now best (1.159, +0.0889R, DD 177R),
  overtaking WTI (1.202 → 1.039). AUDUSD worst on both (0.715, −0.1951R).
* **The acceptance criterion is still met by nothing.** PF ≥ 1.3 is 0/20 on *both* windows, and
  13/20 still have negative expectancy at 365d.
* Caveat worth repeating: cost basis here is slippage 0.5 pip + commission 0, **not** the full
  honest-cost model, so 7/20 is optimistic.
* Comparison trap hit again: reports key results `SWING(H1)` / `DAY_TRADING(M15)` / `SCALP(M5)`.
  A naive loop lets SCALP overwrite SWING and prints nonsense ("20/20 PF≥1.0, mean 1.37").
  **Always filter by style when comparing reports.** Added to MEMORY.md.

## Platform state at 05:0x

* `/api/diagnostics` → OPERATIONAL, MT5 CONNECTED, DATA_FEED STREAMING. Account 101059540
  XMGlobal-MT5 5, **name "Demo Account"**, balance **764.42** (was 779 at session start, then
  771.58) — the two live tickets lost ~14.6 and are closed.
* **The server started 01:47; both fix commits landed later** (955d40c 03:39, 189c1e2 03:58).
  No hot-reload ⇒ **the running platform still has the 7× risk defect and the non-volatility
  sizing.** Needs a restart.
* **Mode is currently PAPER** per `/api/telemetry_state`, although the session *did* route real
  broker tickets at 01:47:25 (#933755581 SELL 0.69 EURUSD#, #933755583 SELL 0.25 AUDUSD#), so
  live execution works when enabled. `/api/action/set_mode` is the switch. Treat "what mode am
  I in" as something to verify, not assume — the log said LIVE and telemetry said PAPER.
* One stale position: `#3927160 [PAPER] BUY 0.01 GOLD.i# @ 2400.0` with **impossible geometry** —
  SL 4930.3 and TP 4395.59 are *both* above entry. For a BUY the stop is effectively acting as a
  second target. This ticket has been ratching SL upward since 01:55 (4293.63 → 4295.31 → 4930.3).
* Two PIDs (25808, 12328) both LISTENING on 8501 — Windows SO_REUSEADDR. Polled 8× and got
  identical PAPER responses, so it is not two disagreeing servers.

## Clock note

The harness `current_time` said "2026-09-15 23:19 GMT+5:30" while `date` said
**Wed Sep 16 05:04 IST 2026** — the injected time was stale by ~5.7 h. Git commit dates
(09-16 04:xx) were right; the harness was wrong. **Don't order events by the injected timestamp.**
