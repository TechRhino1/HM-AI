# 2026-09-19 — UI round 31: nav wrap, tab-bar clipping, phone glass shell

Session started as a continuation of the signal-audit rounds (regime_tagger probes were
mid-flight) but the user redirected hard to the front end:

> "The dashboard UI is still broken and multiple previously requested features are still
> missing… rebuild the navigation bar, which is completely broken (it currently shows two
> lines)… mobile version, redesign… iPhone-style layout with liquid glass…"

## What the user's complaints actually were (all reproduced, none assumed)

Measured in real Chrome via a purpose-written probe, not read off the source.

| Complaint | Verdict | Evidence |
|---|---|---|
| Nav bar "shows two lines" | **TRUE** | `.nav-links-wrapper` rows=2 @1440, rows=3 @390 on /stocks, /india, /options |
| Mobile tab bar broken | **TRUE** | bottom bar scrollWidth 408 (stocks) / 423 (india) / **567** (options) in a 390px viewport; "All View" right edge at 563px |
| Features still missing | **FALSE** | all six items of `docs/PLAN-2026-09-17-six-ui-items.md` are implemented (verified by grep: `modify_pending_order` mt5_client:687 + server:953; `/api/history` filters; copilot panel dashboard.html:869; Analyst/News context strip dashboard.html:229-231; `buildSpace().space` dashboard.js:3897; `setMarkers` dashboard.js:1653) |

**Root cause of the two-line nav — a JS fallback, not a CSS bug.**
`auth.js:137` resolves its host as
`document.querySelector(".nav-links-wrapper") || ".hud-actions" || "header"` and *appends the
account widget into it*. `dashboard.html`, `console.html` and `index.html` declare a real
`#auth-header-widget`; `stocks/india/india_options` did **not**, so four market pills and the
account pill ended up in one flex row whose `flex-wrap: wrap` then broke it.

## What was changed (commit `0e83d5e`, pushed, remote == local)

* `jarvis/ui/templates/{stocks,india,india_options}.html` — declare an explicit
  `#auth-header-widget` as a **sibling** of the nav, so the widget is never injected inside it.
* `jarvis/ui/static/css/hm_ui.css` — `.nav-links-wrapper` pinned to `flex-wrap: nowrap` +
  horizontal scroll; `.mob-tab-btn` changed from `flex: 1 0 auto` to `flex: 1 1 0` with
  `min-width: 0`; new §15 MOBILE SHELL (two-row grid header, glass nav pills, glass tab bar,
  `#hm-main` bottom clearance, safe-area insets, motion inside `no-preference`).

## Verification (all against `.scratch/srv8611.py` on :8611, built from the same tree)

```
verify_ui_layout.js      238/238      verify_dashboard_render.js  209/209
verify_terminal_render.js 55/55       verify_copilot_render.js     23/23
verify_dashboard_nav.js   31/31       audit_endpoints.py           46/46, 0 dead
audit_wiring.py          136/136, 0 broken refs
pytest                   2487 passed, 3 failed, 20 deselected
```

## Findings to carry forward

* **The 3 pytest failures are weekend-fragile tests, not a regression.**
  `tests/test_market_data_independence.py` — three tests assert `freshness == STALE` after
  `time.time()`-relative epochs, but on Saturday `SessionEngine.get_market_trading_status
  ("XAUUSD")` returns `status=CLOSED_WEEKEND, day_of_week=5, is_open=False`, so the classifier
  correctly answers `MARKET_CLOSED` and the warm-up retry path never runs. Confirmed by querying
  the engine directly. **They should pass again Monday.** Worth fixing so the suite is not
  weekend-red — not done here (out of scope, and it touches the market-data path).
* **`/` serves `dashboard.html`, not `index.html`.** `server.py:381` maps
  `"/", "/index.html", "/dashboard", "/dashboard.html"` → `_serve_dashboard_ui()`; `/classic` →
  `index.html`. Probing "the root page" and expecting the terminal is a trap — the first probe of
  this session did exactly that and reported a missing `.mobile-nav-bar` that was never there.
* **Load order decides which sheet wins, and it differs per page.** The three market pages load
  `page.css` → `auth.css` → `hm_ui.css` (design system last, so it can override), but
  `dashboard.html` loads `hm_ui.css` **first**, then `theme_terminal.css`. Any rule added to
  `hm_ui.css` therefore governs the market pages and does *not* govern the dashboard. Read the
  `<link>` order of the specific page before assuming an override lands.
* **A user's "it's broken" and a measurement disagreeing is itself the finding.** The rail
  *looked* overlapped in a screenshot at 1440 (account pill apparently on top of the session
  badge). Measuring the geometry proved zero overlap at every width — it was the rail's secondary
  band wrapping to a second row, left-aligned. Do not "fix" a layout defect that a bounding-box
  comparison cannot reproduce.

## Standing operational item (unchanged)

`data/jarvis_drawdown_state.db` still holds `daily_start_equity = 777.0` against
`peak_equity = 10150.0`, so the 4% daily-loss cap remains inert. The dashboard shows this as
`EQUITY 777.00 USD`. Reset the row or wait for UTC midnight.
