# 2026-09-21

## Round 42 — UI restyle (dashboard)

Closed the dashboard Open positions from the reference image (column structure, typography, P&L coloring, action buttons). Removed the Pending orders section entirely from the new dashboard (HTML + JS + CSS + tests; server endpoints kept because the classic terminal at /classic still uses them). Fixed the nav bar so active/hover states are clearly distinguishable. All verifiers green: verify_dashboard_render 206/206, verify_dashboard_nav 30/31 (1 pre-existing data issue), verify_copilot 23/23, verify_terminal 55/55, verify_phone_nav 13/13, verify_ui_layout 238/238; backend 3005 passed; no console warnings.

D1 (execution_mode) still uncommitted; mutation 4 guard is genuinely redundant on SQLite and was kept as documentation of contract.

## Round 43 — iOS-style positions page (/positions)

New standalone page implementing the Open positions panel with iOS design language and Bootstrap 5 responsive scaffolding. Three new files: jarvis/ui/templates/positions.html, jarvis/ui/static/css/positions.css (242 lines, shared design rules — no inline duplication), jarvis/ui/static/js/positions.js (334 lines, fixture data + interactions). Route added to jarvis/api/server.py at /positions (also /positions.html).

Design: system-like typography (-apple-system / SF Pro), rounded cards (14px), iOS segmented control, sticky nav bar, bottom tab bar on <768px only, iOS toggle switch, bottom-sheet modal with grabber, safe-area insets (env(safe-area-inset-*)), light/dark via prefers-color-scheme. Bootstrap 5 CDN for the responsive grid + breakpoints.

Features implemented from the reference image: tab bar (OPEN/HISTORY/PENDING with counts), FLATTEN ALL button, total P&L with sign and color, table with SYMBOL/SIDE/VOL/ENTRY/NOW/SL/TP/P&L/ACTIONS, Close/50%/Modify buttons per row. Plus: Modify action sheet (SL/TP/trailing toggle/trail pips), search filter, history tab with duration/opened/closed, PENDING tab empty state, iOS toasts, flatten confirmation.

Responsive verified at 375px (phone), 768px (tablet), 1440px (desktop): all show overflow=0, zero console errors, zero warnings. Phone uses stacked cards + tab bar; tablet+ uses row table with tab bar hidden.

Screenshots: .scratch/shots/pos-{1..8}-*.png.

## Round 44 — Dashboard Open positions: Open / History / Pending tabs

User asked to add History and Pending tabs to the dashboard's Open positions panel so the reference image's tab bar is visible on /. Initially the panel head carried `<h2>Open positions</h2>` + `#pos-count` + a `#flatten-all` button.

Three template + JS + CSS files:

- `jarvis/ui/templates/dashboard.html` — panel head now `<h2>Positions</h2>` (was "Open positions" — the panel now hosts three lists, so the title matches /positions) plus a `.tt-pos-tabs` tablist (Open / History / Pending, each with a `.tt-pos-tab__count` badge), plus the total P&L and Flatten all. The table got `id="pos-table"` and the thead `id="pos-thead"`.
- `jarvis/ui/static/css/theme_terminal.css` — added `.tt-pos-tabs`, `.tt-pos-tab` (pill, `aria-selected="true"` = solid accent + inverted text — matches the nav bar's active state), `.tt-pos-tab__count`.
- `jarvis/ui/static/js/dashboard.js` — added `state.posTab: 'open'`, `state.posPending: []`, `state.historyLoading: false`. New functions: `renderHistoryInPosPanel()`, `renderPendingInPosPanel()`, `pendingTypeNameLocal()` (MT5 enum -> string), `pendingAge()`, `cancelPendingFromPanel()`, `loadPendingForTab()`, `setPosTab()` (rewrites thead, syncs `aria-selected`, hides OPEN-only total/Flatten on non-OPEN tabs, dispatches to the right renderer). boot() now calls `setPosTab('open'); loadPendingForTab();` after `loadHistory()`. The telemetry handler now dispatches to the active tab's renderer instead of unconditionally calling `renderPositions()`. OPEN count badge refreshed in telemetry so it stays current while another tab is active. Flatten-all now counts only the attempted set (positions with a ticket).

Bug found and fixed: the periodic telemetry refresh was calling `renderPositions()` unconditionally, overwriting the Pending tab's body with the OPEN empty state every tick. Discovered via puppeteer snap showing `state="empty", title="No open positions"` for the Pending tab even after switching. Fix: telemetry dispatch by `state.posTab`.

Verifier coverage added to `tools/verify_dashboard_render.js`: structural check for the three `data-pos-tab` buttons + `pos-table` + `pos-thead` + `flatten-all` ids; structural check that `setPosTab` / `renderHistoryInPosPanel` / `renderPendingInPosPanel` / `loadPendingForTab` exist; boot() check that `setPosTab('open')` and `loadPendingForTab()` are wired.

Tests: verify_dashboard_render 209/209 (was 206; +3 tab checks), verify_terminal_render 55/55, verify_copilot_render 23/23, verify_phone_nav 13/13, verify_dashboard_nav 30/31 (1 pre-existing Leverage data issue), verify_ui_layout 238/238. pytest tests/test_ui_wiring.py 24/24.

Real-browser verification (puppeteer, port 8501): sequence open -> history -> pending -> open. open thead "SYMBOL SIDE VOL ENTRY NOW SL TP P&L ACTIONS" + "No open positions"; history thead "SYMBOL SIDE VOL ENTRY EXIT P&L CLOSED ACTIONS" + 269 rows; pending thead "SYMBOL TYPE VOL PRICE SL TP AGE ACTIONS" + "No working orders · Place a limit or stop…". `tabErrors: []`. Horizontal overflow = 0 at 375/768/1440. Screenshots: `.scratch/shots/tab-{1-open,history,pending}.png` and `tab-open-{375,768,1440}.png`.

Endpoints reused: `/api/pending_orders` (mt5_client.get_pending_orders), `/api/action/cancel_pending_order`, `/api/history` (state.history). Cancel button uses the existing confirm-then-toast pattern shared with Close.

## Round 45 — iOS mobile redesign of the dashboard (committed 22668d4)

Phone-only iOS layer, desktop untouched: jarvis/ui/static/css/ios_mobile.css (all rules inside @media (max-width:767px)), stylesheet link last in dashboard.html, large-title node #ios-largetitle, setView() mirrors active view, dashboard.js syncTabBarHost() reparents .tt-tabs onto <body> on phones (because .tt-rail's backdrop-filter is the fixed-position containing block). Login smoke test passes on 375x812 puppeteer: overflow 0, tab bar FIXED_BOTTOM, drawer+auth as bottom sheets, required-field + invalid-cred + loading + reload-persist all good. Caveat: localhost auto-auth means the test forces logged-out state; no post-login redirect (modal closes, in-place refresh).

## Round 46 — P1 backlog fixes (C8 / A1 / A2)

A1 (three drifted _radar_sort_key copies): consolidated into jarvis/application/radar_sort.py::radar_sort_key. The orchestrator copy returned a 5-tuple with utility_score; the two server.py copies returned 4-tuples; only the endpoint fell back to status_label. Two producers write radar_opportunities (orchestrator's to_radar_item() and server.py bg-loop dict) and one consumer re-sorts on read. Now all three sites use the one key. Tests: 48 targeted pass; 168 realized_pnl/radar tests pass.

C8 (gross vs net P&L): database.py used only exit_deal.profit (gross); state_synchronizer.py:73 publishes net (profit+swap+commission) on trade_closed. Fixed database.py to compute net = exit_profit + entry.commission + entry.swap + exit.commission + exit.swap (entry.profit is 0 for an opening deal), reconciling realized_pnl with the trade_closed figure. No tests assert the old gross value.

A2 (parallel scan no-op): SignalScanner.parallel_analysts defaulted False and no caller ever set it True, so the cluster's ThreadPoolExecutor branch was dead for the scan; the two cluster modes only differ in failure semantics, and the scan already wraps the pipeline in try/except per bar. Removed the dead flag; the scan now constructs ParallelAnalystCluster() (production parallel mode) so it runs the same analyst path as live decisions — the scanner's whole purpose. No caller passed the flag; tests hit the cluster directly so they stay green.

Full suite baseline 3005 passed before changes; targeted + 168 realized_pnl tests green after. Build compiles (compileall exit 0). Commit pending.
